# 硕士论文对比验证实验设计方案

## 项目背景与研究目标

**项目**: GltfInstancingAndTilingTool - GLTF/GLB模型实例化检测与3D Tiles瓦片化工具
**核心问题**: 如何高效地在Web端渲染大规模城市级BIM数据（如Revit导出的MEP模型）
**技术路线**:
1. 实例化检测 → GPU实例化渲染 (`EXT_mesh_gpu_instancing`)
2. 语义+几何驱动的5级LOD生成
3. 四叉树HLOD空间划分

---

## 实验设计框架

### 总体设计原则

| 原则 | 说明 |
|------|------|
| **对照实验** | 每个实验设置对照组和实验组，控制单一变量 |
| **重复测量** | 每组实验至少运行3-5次，取平均值和方差 |
| **多数据集** | 使用不同类型、规模的BIM数据集验证泛化性 |
| **统计显著性** | 使用合适的统计方法验证结果差异的显著性 |

---

## 实验一：实例化检测策略对比实验

### 实验目的
验证不同实例化检测策略对压缩率、渲染性能和视觉质量的影响。

### 实验变量

| 策略名称 | Similarity Threshold | Instance Limit | 说明 |
|----------|---------------------|----------------|------|
| **Strict** | 0.99 (严格) | 5 | 基线对照组 - 高相似度才合并 |
| **Moderate** | 0.95 (中等) | 3 | 允许轻微几何差异 |
| **Lenient** | 0.90 (宽松) | 2 | 允许较大几何差异 |

### 测量指标

| 指标类别 | 具体指标 | 数据来源 |
|----------|----------|----------|
| **压缩性能** | 文件大小缩减率 (%) | `01_instancing/analysis/instancing.csv` |
| | 节点减少百分比 (%) | `01_instancing/analysis/instancing.csv` |
| | 实例化率 (%) | `01_instancing/analysis/instancing.csv` |
| **渲染性能** | Draw Calls减少数 | 计算: Initial Meshes - Final Meshes |
| | GPU内存占用 | 运行时测量 |
| **视觉质量** | 几何误差 (Hausdorff距离) | 需新增计算 |
| | 视觉相似度 (SSIM) | 需渲染截图对比 |

### 实验步骤

```
1. 准备数据集 (3种不同类型BIM模型)
2. FOR each 数据集:
3.   FOR each 策略 (Strict/Moderate/Lenient):
4.     运行工具生成实例化GLB
5.     记录 01_instancing/analysis/instancing.csv
6.     渲染截图用于视觉质量评估
7.   END FOR
8. END FOR
9. 统计分析并生成对比图表
```

### 预期结果分析

- **假设1**: 容差越大，实例化率越高，文件压缩率越高
- **假设2**: 容差越大，几何误差越大，视觉质量越低
- **假设3**: 存在最优容差平衡点（高压缩率 + 可接受视觉损失）

---

## 实验二：LOD生成策略对比实验

### 实验目的
对比实例化LOD（语义驱动）与非实例化LOD（几何简化）在不同层级的性能与质量表现。

### 实验设计

#### 2.1 实例化LOD层级分析

**测试层级**: LOD5(原始) → LOD4(Variant) → LOD3(Class) → LOD2(Abstract) → LOD1(Proxy)

**测量指标**:

| 指标 | 说明 | 数据来源 |
|------|------|----------|
| 文件大小 (MB) | 每层级GLB大小 | `02_instance_lod/analysis/instance_lod.csv` |
| 顶点数缩减率 (%) | (原始-当前)/原始 | `02_instance_lod/analysis/instance_lod.csv` |
| 唯一Mesh数 | 聚合后的Mesh种类 | `02_instance_lod/analysis/instance_lod.csv` |
| 几何误差 | 与原始模型的偏差 | 需新增计算 |

#### 2.2 实例化LOD vs 非实例化LOD 对比

| 对比维度 | 实例化LOD | 非实例化LOD |
|----------|-----------|-------------|
| LOD策略 | 语义+几何驱动5级 | 网格简化4级 |
| 核心算法 | Family/Category聚类 | meshoptimizer简化 |
| 代表体选择 | 体积平均/顶点最少 | - |

**测量指标**:

| 指标 | 实例化LOD | 非实例化LOD |
|------|-----------|-------------|
| 文件大小缩减率 | `02_instance_lod/analysis/instance_lod.csv` | `02_non_instance_lod/analysis/non_instance_lod_analysis.csv` |
| 三角形缩减率 | `02_instance_lod/analysis/instance_lod.csv` | `02_non_instance_lod/analysis/non_instance_lod_analysis.csv` |
| 渲染Batch数 | 计算 | 计算 |
| LOD切换平滑度 | 视觉评估 | 视觉评估 |

### 实验步骤

```
1. 选择代表性BIM数据集
2. 生成实例化LOD (LOD1-LOD5)
3. 生成非实例化LOD (LOD0-LOD3)
4. FOR each LOD层级:
5.   测量文件大小、顶点数、三角形数
6.   计算几何误差 (vs 原始模型)
7.   记录渲染性能指标
8. END FOR
9. 绘制LOD性能曲线 (文件大小vs误差)
```

### 预期结果分析

- **假设1**: 实例化LOD在低层级(LOD1-2)有更大文件压缩优势
- **假设2**: 非实例化LOD在高层级(LOD3+)保留更多几何细节
- **假设3**: 实例化LOD切换更平滑（语义一致性）

---

## 实验三：HLOD空间划分策略评估

### 实验目的
评估四叉树HLOD的不同参数配置对空间查询效率和渲染性能的影响。

### 实验变量

| 参数 | 测试值1 | 测试值2 | 测试值3 | 说明 |
|------|---------|---------|---------|------|
| **Max Depth** | 4 | 6 | 8 | 四叉树最大深度 |
| **Max Objects/Tile** | 30 | 50 | 100 | 每瓦片最大对象数 |
| **Geometric Error** | 1000 | 2000 | 5000 | 几何误差阈值 |

### 测量指标

| 指标类别 | 具体指标 | 数据来源 |
|----------|----------|----------|
| **空间效率** | 总瓦片数 | `03_hlod/hlod_analysis.csv` |
| | 平均每瓦片对象数 | 计算 |
| | 瓦片大小均衡性 (CV) | 计算变异系数 |
| **渲染性能** | 视锥裁剪效率 | 运行时测量 |
| | LOD切换延迟 | 运行时测量 |
| | 内存占用峰值 | 运行时测量 |

### 实验步骤

```
1. 选择大规模城市级BIM数据集
2. FOR each Max Depth (4, 6, 8):
3.   FOR each Max Objects/Tile (30, 50, 100):
4.     生成四叉树HLOD
5.     记录 03_hlod/hlod_analysis.csv
6.     模拟相机飞行路径测量渲染性能
7.   END FOR
8. END FOR
9. 分析参数组合与性能的关系
```

### 预期结果分析

- **假设1**: 深度越大，瓦片越小，裁剪效率越高，但tileset.json越复杂
- **假设2**: 每瓦片对象数适中(50)时性能最佳（平衡批量化和剔除效率）
- **假设3**: 存在最优参数组合与数据规模相关

---

## 实验四：端到端综合性能对比

### 实验目的
对比完整流程中不同优化策略组合的实际渲染性能和用户体验。

### 对比策略

| 编号 | 策略名称 | 描述 |
|------|----------|------|
| **A** | 原始GLB (Baseline) | 不做任何优化，直接加载原始模型 |
| **B** | 仅实例化 | 使用最优容差进行实例化检测 |
| **C** | 实例化 + LOD | 实例化 + 5级语义LOD |
| **D** | 实例化 + HLOD | 实例化 + 四叉树空间划分 |
| **E** | 完整方案 | 实例化 + LOD + HLOD (混合策略) |

### 测量指标

| 指标类别 | 具体指标 | 测量方法 |
|----------|----------|----------|
| **存储效率** | 总文件大小 | 统计所有输出文件 |
| | 网络传输量 | 模拟网络加载 |
| **加载性能** | 初始加载时间 | Chrome DevTools Performance |
| | 首屏渲染时间 | Performance API |
| **渲染性能** | 平均帧率 (FPS) | Chrome FPS计数器 |
| | GPU Draw Calls | WebGL Inspector/Spector.js |
| | GPU内存占用 | Chrome Task Manager |
| | CPU占用率 | Chrome Task Manager |
| **用户体验** | 交互响应延迟 | 输入事件处理时间 |
| | 视觉质量评分 | 用户主观评分 (1-5分) |

### 测试场景

设计3种典型交互场景：

1. **场景A - 全局概览**: 远距离观察整个场景
   - 评估: HLOD根节点加载速度、整体渲染帧率

2. **场景B - 区域浏览**: 中等距离漫游特定区域
   - 评估: LOD切换平滑度、瓦片流式加载效率

3. **场景C - 细节查看**: 近距离查看单个构件
   - 评估: 最高精度模型加载、实例化渲染细节

### 实验步骤

```
1. 选择3个不同规模的数据集 (小型/中型/大型)
2. FOR each 数据集:
3.   FOR each 策略 (A, B, C, D, E):
4.     生成对应的输出文件
5.     FOR each 测试场景 (A, B, C):
6.       运行自动化性能测试脚本
7.       记录所有性能指标
8.     END FOR
9.   END FOR
10. END FOR
11. 统计分析并绘制雷达图对比
```

### 预期结果分析

- **假设1**: 仅实例化(B)在中等距离场景有最佳性能提升
- **假设2**: 完整方案(E)在综合场景表现最优
- **假设3**: 策略组合存在协同效应（1+1>2）

---

## 实验五：非均匀缩放实例化验证

### 实验目的
验证允许非均匀缩放实例化对特定类型BIM构件的检测率提升。

### 实验设计

**测试对象**: 具有非均匀缩放特征的BIM构件
- 通风管道 (不同长宽比)
- 电缆桥架 (不同长度)
- 结构梁 (不同截面)

**对比设置**:

| 设置 | 允许非均匀缩放 | 说明 |
|------|----------------|------|
| **Uniform** | false | 仅允许均匀缩放实例化 |
| **Non-uniform** | true | 允许非均匀缩放实例化 |

### 测量指标

| 指标 | 计算方法 |
|------|----------|
| 额外检测到的实例组数 | Non-uniform - Uniform |
| 实例化率提升 (%) | (Final_非均匀 - Final_均匀) / Initial |
| 视觉质量损失 | 与原始模型对比 |

---

## 实验六：跨GLB HLOD构建策略对比

### 实验目的
验证多GLB文件**合并构建统一HLOD**与**独立构建各自HLOD**两种策略在空间一致性、渲染效率和加载性能上的差异。

### 实验背景
在城市级BIM应用中，通常会有多个GLB文件（如不同楼层、不同专业）：
- **跨GLB HLOD**：将所有GLB合并到一个四叉树中，构建统一的空间层次结构
- **独立HLOD**：每个GLB单独构建自己的HLOD，多个tileset.json

### 实验设计

#### 对比策略

| 策略 | 名称 | 描述 | 适用场景 |
|------|------|------|----------|
| **A** | Merged HLOD | 多个GLB合并构建统一四叉树HLOD | 同一区域内紧密关联的模型 |
| **B** | Separate HLOD | 每个GLB独立构建HLOD | 相对独立分布的模型 |

#### 测试数据集配置

**数据集类型1：多楼层建筑**
- 输入：20个GLB文件（每层1个）
- 空间分布：垂直堆叠，XY范围重叠
- 预期：合并策略更优（空间连续性）

**数据集类型2：多专业分散**
- 输入：5个GLB文件（建筑、结构、MEP等）
- 空间分布：同一区域，专业分区
- 预期：合并策略更优（避免瓦片重叠）

**数据集类型3：远距离分散**
- 输入：4个GLB文件（不同建筑/地块）
- 空间分布：XY距离 > 500m
- 预期：独立策略可能更优

### 测量指标

| 指标类别 | 具体指标 | 说明 | 预期优势方 |
|----------|----------|------|------------|
| **空间结构** | 总瓦片数 | HLOD树的总节点数 | 视分布而定 |
| | 树深度分布 | 各叶子节点的深度方差 | Merged（更均衡） |
| | 瓦片重叠数 | 空间上重叠的瓦片数量 | Merged（0重叠） |
| | AABB利用率 | 包围盒空间利用率 | Merged |
| **文件效率** | tileset.json总大小 | 描述文件大小 | Merged（单文件） |
| | 根节点GeometricError | 全局误差控制 | Merged |
| | 重复资源数 | 跨GLB的重复材质/纹理 | Merged（可去重） |
| **渲染性能** | 视锥裁剪批次 | 单次视锥查询返回瓦片数 | Merged（更精确） |
| | LOD切换一致性 | 相邻区域LOD层级差异 | Merged（更平滑） |
| | Draw Calls | 渲染调用次数 | 视场景而定 |
| **加载性能** | 初始请求数 | 加载根节点所需请求 | Merged（1次） vs Separate（N次） |
| | 首瓦片加载时间 | 首屏可见瓦片加载耗时 | 视网络而定 |
| | 内存占用峰值 | 运行时内存使用 | 视策略而定 |

### 实验步骤

```
1. 准备测试数据集（多楼层/多专业/远距离三种）
2. FOR each 数据集:
3.   # 策略A：合并HLOD
4.   将所有GLB作为同一批次输入工具
5.   构建统一四叉树HLOD
6.   输出：merged_tileset.json + 瓦片文件
7.   记录所有指标
8.
9.   # 策略B：独立HLOD
10.  FOR each GLB文件:
11.    单独构建HLOD
12.    输出：tileset_{i}.json + 瓦片文件
13.  END FOR
14.  记录所有指标（汇总）
15.
16.  生成对比报告
17. END FOR
```

### 输出文件结构

```
06_CrossGLBHLOD/
├── README.md
│
├── dataset_多楼层建筑/
│   ├── input/                          # 原始GLB文件
│   │   ├── Floor_01.glb
│   │   ├── Floor_02.glb
│   │   └── ...
│   │
│   ├── A_MergedHLOD/                   # 策略A：合并构建
│   │   ├── config.json
│   │   ├── merged_tileset.json         # 统一的tileset
│   │   ├── root.glb
│   │   ├── L1_0_0.glb
│   │   ├── L1_0_1.glb
│   │   └── ...
│   │
│   ├── B_SeparateHLOD/                 # 策略B：独立构建
│   │   ├── Floor_01/
│   │   │   ├── tileset.json
│   │   │   └── ...
│   │   ├── Floor_02/
│   │   │   ├── tileset.json
│   │   │   └── ...
│   │   └── combined_tileset.json       # 可选：聚合引用
│   │
│   └── comparison/
│       ├── spatial_analysis.csv        # 空间结构对比
│       ├── performance_metrics.csv     # 性能指标对比
│       └── charts/
│           ├── tile_distribution.png   # 瓦片分布对比
│           └── load_time_comparison.png
│
├── dataset_多专业分散/
└── dataset_远距离分散/
```

### 关键发现预期

| 场景 | 推荐策略 | 原因 |
|------|----------|------|
| 垂直堆叠（多楼层） | **Merged** | 避免同一XY位置多个瓦片，LOD切换一致 |
| 同一区域多专业 | **Merged** | 消除专业间瓦片重叠，统一空间划分 |
| 远距离分散 | **Separate** | 减少单个tileset复杂度，按需加载 |
| 超大规模（>100GLB） | **Hybrid** | 邻近GLB合并组，组间独立 |

### 工程建议输出

基于实验结果，提供决策建议：

```markdown
## 跨GLB HLOD构建决策指南

1. 计算GLB集合的AABB重叠度
   - 重叠度 > 80% → 推荐 Merged
   - 重叠度 < 30% → 推荐 Separate
   - 30%-80% → 需要进一步分析

2. 评估空间连续性
   - 相邻GLB中心距离 < 平均尺寸 → Merged
   - 相邻GLB中心距离 > 3倍平均尺寸 → Separate

3. 性能权衡
   - 网络延迟高 → Merged（减少请求数）
   - 内存受限 → Separate（按需加载）
```

---

## 数据集设计

### 数据集选择标准

| 属性 | 说明 |
|------|------|
| **类型多样性** | 建筑、结构、MEP等不同专业 |
| **规模差异** | 小型(1000构件)、中型(10000构件)、大型(100000+构件) |
| **实例化潜力** | 高重复性(如住宅) vs 低重复性(如异形建筑) |
| **来源真实** | 实际工程项目导出的Revit模型 |

### 推荐数据集

| 编号 | 名称 | 规模 | 类型 | 实例化潜力 | 用途 |
|------|------|------|------|------------|------|
| D1 | 标准层住宅 | ~3000构件 | 住宅 | 高(90%+) | 实验1,2,4 |
| D2 | 办公楼MEP | ~15000构件 | MEP | 中(60%+) | 实验1,2,3 |
| D3 | 医院整体 | ~80000构件 | 综合 | 中(50%+) | 实验3,4,6 |
| D4 | 异形展览馆 | ~10000构件 | 异形 | 低(20%+) | 边界测试 |
| D5 | 多楼层建筑 | ~60000构件 | 住宅 | 高(80%+) | 实验6 |
| D6 | 园区多建筑 | ~120000构件 | 综合 | 中(40%+) | 实验6 |

---

## 数据分析与可视化

### 统计分析方法

| 分析类型 | 方法 | 用途 |
|----------|------|------|
| 描述性统计 | 均值、标准差、CV | 数据基本情况 |
| 对比分析 | 柱状图、箱线图 | 多策略对比 |
| 趋势分析 | 折线图、散点图 | LOD层级变化趋势 |
| 相关性分析 | Pearson/Spearman相关系数 | 参数与性能关系 |
| 显著性检验 | t-test / ANOVA | 验证差异显著性 |

### 建议图表类型

1. **实验1**: 分组柱状图 (容差 vs 压缩率/误差)
2. **实验2**: 双Y轴折线图 (LOD层级 vs 文件大小/顶点数)
3. **实验3**: 热力图 (Max Depth × Max Objects vs 性能)
4. **实验4**: 雷达图 (多维度策略对比)
5. **实验6**: 堆叠柱状图 (瓦片分布对比) + 网络请求瀑布图
6. **综合**: 桑基图 (数据流与优化效果)

---

## 实验实施计划

### 阶段1: 数据准备 (1周)
- [ ] 收集并整理4个数据集
- [ ] 验证数据格式和完整性
- [ ] 建立数据版本管理

### 阶段2: 工具配置 (3天)
- [ ] 确定实验参数配置文件模板
- [ ] 编写批量实验脚本
- [ ] 设置输出目录结构

### 阶段3: 实验执行 (2周)
- [ ] 实验1: 实例化检测策略 (3天)
- [ ] 实验2: LOD策略对比 (3天)
- [ ] 实验3: HLOD参数评估 (3天)
- [ ] 实验4: 端到端性能 (4天)
- [ ] 实验6: 跨GLB HLOD对比 (2天)
- [ ] 数据校验与重复实验 (1天)

### 阶段4: 数据分析 (1周)
- [ ] 整理所有CSV数据
- [ ] 计算派生指标
- [ ] 生成统计图表
- [ ] 显著性检验

### 阶段5: 论文撰写 (2周)
- [ ] 实验章节撰写
- [ ] 结果讨论与分析
- [ ] 图表优化

---

## 已有输出文件利用

项目已自动生成的CSV文件可直接用于实验:

| 文件路径 | 包含数据 | 适用实验 |
|----------|----------|----------|
| `01_instancing/analysis/instancing.csv` | 实例化前后对比指标 | 实验1,4 |
| `02_instance_lod/analysis/instance_lod.csv` | 5级实例化LOD指标 | 实验2,4 |
| `02_non_instance_lod/analysis/non_instance_lod_analysis.csv` | 非实例化LOD指标 | 实验2 |
| `03_hlod/hlod_analysis.csv` | 四叉树HLOD指标 | 实验3,6 |
| `cross_glb_hlod_analysis.csv` | 跨GLB HLOD对比指标 | 实验6 |
| `*_results.csv` | 详细分组信息 | 深入分析 |

---

## 需要补充的工作

### 新增功能 (可选)

1. **几何误差计算**: Hausdorff距离或顶点均方误差
2. **批量实验脚本**: Python脚本自动化运行多组实验
3. **可视化工具**: 结果对比图表自动生成

### 外部工具

| 工具 | 用途 |
|------|------|
| **CesiumJS** | Web端渲染性能测试 |
| **Chrome DevTools** | 性能剖析 |
| **Spector.js** | WebGL调用分析 |
| **Python (pandas/matplotlib)** | 数据统计与可视化 |

---

## 论文结构建议

```
第四章 实验与评估
  4.1 实验环境与数据集
      4.1.1 软硬件环境
      4.1.2 测试数据集
      4.1.3 评价指标
  4.2 实例化检测实验
      4.2.1 实验设计
      4.2.2 结果分析
  4.3 LOD生成实验
      4.3.1 实验设计
      4.3.2 结果分析
  4.4 HLOD策略实验
      4.4.1 实验设计
      4.4.2 结果分析
  4.5 综合性能对比
      4.5.1 实验设计
      4.5.2 结果分析
  4.6 跨GLB HLOD策略实验
      4.6.1 实验设计
      4.6.2 结果分析
  4.7 本章小结
```

---

## 关键科学问题

1. **最优容差如何确定?** → 实验1中寻找压缩率与视觉质量的平衡点
2. **语义LOD vs 几何LOD优劣?** → 实验2对比分析
3. **HLOD参数如何配置?** → 实验3确定最优参数组合
4. **优化策略如何组合?** → 实验4验证协同效应
5. **跨GLB构建策略如何选择?** → 实验6给出决策依据

---

*该实验设计方案充分利用了项目已有的实验框架和CSV输出功能，通过系统化的对比实验验证各优化策略的有效性，为硕士论文提供充分的实验数据支撑。*
