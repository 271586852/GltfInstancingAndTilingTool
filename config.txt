# Example configuration file for experiment mode
# This configuration enables experiment mode to organize outputs into three comparison folders

# Input directory containing GLB files
input_directory = D:\dissertationProject\Data\BIMData\0HRZBSingleViewerExport

# Output directory where results will be organized
output_directory = D:\dissertationProject\Data\BIMData\output

# Enable experiment mode to organize outputs into three folder groups
enable_experiment_mode = true

# Use symbolic links instead of copying files (saves disk space)
# Set to false if you want to copy files instead of linking
use_symbolic_links = true

# Custom experiment folder names (optional)
experiment1_name = experiment1_baseline_comparison
experiment2_name = experiment2_lod_comparison
experiment3_name = experiment3_mixed_hlod

# Basic instancing detection parameters
tolerance = 0.01
normal_tolerance = 0.1
instance_limit = 3
allow_non_uniform_scale_instancing = true

# Enable LOD generation for experiments
enable_lod_generation = true
lod_level_count = 5
target_screen_sse = 16.0

# Enable non-instanced LOD generation for comparison
enable_non_instanced_lod_generation = true
non_instanced_lod_level_count = 3
non_instanced_lod_ratio = 0.5
enable_non_instanced_lod_instancing = true

# Enable quadtree HLOD for experiment3
enable_quadtree = true
quadtree_max_depth = 6
quadtree_max_objects_per_tile = 50

# HLOD-specific instancing parameters (can be different from Stage 1)
hlod_tolerance = 0.02
hlod_normal_tolerance = 0.15
hlod_instance_limit = 2
hlod_allow_non_uniform_scale_instancing = true

# Mesh segmentation will be automatically enabled for quadtree if not explicitly set
mesh_segmentation = true

# Enable semantic and geometric checks for better LOD quality
enable_semantic_check = true
enable_geometric_check = true