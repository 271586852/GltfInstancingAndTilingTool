# 顾及多级LOD分层实例特征的复杂BIM模型轻量化方法

## 第三章 融合哈希算法的多维度信息的相似性判别模型

本章针对BIM模型实例化检测中多源异构信息的融合难题，提出**融合哈希算法的多维度信息的相似性判别模型**。该模型将语义哈希、材质哈希与Hausdorff几何相似度有机结合，构建语义-材质-几何三层筛选流程，实现高效、可配置的实例化等价判定。本章首先给出实例化判定的形式化定义，继而阐述哈希算法在多维度信息融合中的核心作用，最后详述各层判定的技术实现。

### 3.1 实例化判定的形式化定义与流程框架

#### 3.1.1 BIM构件等价关系定义

在BIM模型中，实例化检测的本质是识别**几何等价**且**语义相容**的构件集合。为此，首先建立构件等价关系的形式化定义。

**定义3.1（几何等价）** 设两个BIM构件 $m_1$ 和 $m_2$ 的几何表面分别为 $S_1$ 和 $S_2$，若存在刚体变换 $T \in SE(3)$（包含旋转和平移），使得两个表面在Hausdorff距离意义下满足容差约束：

$$d_H(S_1, T(S_2)) = \max\left\{\sup_{p \in S_1} \inf_{q \in S_2} \|p - T(q)\|, \sup_{q \in S_2} \inf_{p \in S_1} \|T^{-1}(p) - q\|\right\} \leq \epsilon_g$$

则称 $m_1$ 和 $m_2$ 几何等价，记作 $m_1 \stackrel{g}{\sim} m_2$，其中 $\epsilon_g$ 为几何容差阈值。

**定义3.2（语义等价）** 设构件 $m$ 的语义属性为五元组 $S(m) = (C, F, T, L, M)$，其中：
- $C$：构件类别（Category），如墙、门、窗
- $F$：族名称（Family）
- $T$：类型名称（Type）
- $L$：层级信息（Level）
- $M$：材质信息（Material）

若两个构件 $m_1, m_2$ 满足：

$$C(m_1) = C(m_2) \wedge F(m_1) = F(m_2) \wedge T(m_1) = T(m_2)$$

则称二者语义等价，记作 $m_1 \stackrel{s}{\sim} m_2$。

**定义3.3（变换等价）** 设构件 $m_1, m_2$ 的局部到世界坐标变换矩阵分别为 $\mathbf{M}_1, \mathbf{M}_2 \in \mathbb{R}^{4 \times 4}$。对变换矩阵进行分解：

$$\mathbf{M} = \mathbf{T} \cdot \mathbf{R} \cdot \mathbf{S}$$

其中 $\mathbf{T}$ 为平移，$\mathbf{R}$ 为旋转，$\mathbf{S} = \text{diag}(s_x, s_y, s_z)$ 为缩放。若满足：

$$\|s_x - s_y\| + \|s_y - s_z\| + \|s_z - s_x\| \leq \delta_{non-uni}$$

则称缩放类型为**均匀缩放**；否则为**非均匀缩放**。变换等价要求缩放类型一致且比例因子在容差范围内。

**定义3.4（实例化等价）** 综合上述定义，构件 $m_1, m_2$ 可实例化的充要条件为：

$$m_1 \stackrel{inst}{\sim} m_2 \iff m_1 \stackrel{g}{\sim} m_2 \wedge m_1 \stackrel{s}{\sim} m_2 \wedge \text{TransformCompatible}(m_1, m_2)$$

#### 3.1.2 语义-材质-几何三层判定流程

基于上述定义，提出**语义-材质-几何三层筛选**的判定流程：

```
┌─────────────────────────────────────────────────────────────────────┐
│                    实例化判定总体流程                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  │ 输入BIM构件  │───→│  语义预筛选   │───→│  材质过滤    │───→│ Hausdorff 几何│
│  │   集合 M    │    │  (粗粒度)     │    │ (可选)       │    │ 相似度(细粒度)│
│  └──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
│                               │                  │                  │
│                               ↓                  ↓                  ↓
│                        ┌──────────────┐    ┌──────────────┐   ┌──────────────┐
│                        │ 语义分组集合 │    │ material_    │   │ 点云采样+     │
│                        │ G₁,G₂...或   │    │ filter_mode: │   │ 双向Hausdorff │
│                        │ __GLOBAL__   │    │ none/hash/   │   │ s=1/(1+d_H)   │
│                        └──────────────┘    │ index        │   └──────┬───────┘
│                                            └──────────────┘          │
│                                                    │                  │
│                                                    └────────┬────────┘
│                                                             ↓         │
│                                                    ┌──────────────┐   │
│                                                    │ s≥τ 实例化分组│   │
│                                                    │   I₁,I₂...   │   │
│                                                    └──────────────┘   │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

**第一层：语义预筛选（语义哈希）** 通过 `semantic_hash_fields` 可配置字段（如 category、family、type）进行分组，`buildSemanticHashKey` 将各字段拼接为分组键；若为空则所有 mesh 归入 `__GLOBAL__` 单组。语义哈希实现粗粒度预筛选，时间复杂度 $O(n \log n)$，将问题规模降至各组内 $n_i \ll n$，为后续材质过滤与几何比较奠定基础。

**第二层：材质过滤（材质哈希）** 通过 `material_filter_mode` 控制：`none` 不过滤；`hash` 时仅当 `getMeshMaterialHash`（PBR 因子、emissiveFactor、alphaMode、doubleSided）相同才进入几何比较，实现材质维度的哈希等价类划分；`index` 时仅当材质索引相同才比较。材质哈希避免几何相似但材质不同的构件（如同形状不同颜色的门）被误判为实例。

**第三层：Hausdorff 几何相似度判定** 通过点云采样（`hausdorff_max_sample_points`）与双向 Hausdorff 距离计算相似度 $s = 1/(1+d_H)$，$s \geq \tau$ 时判定为几何等价，时间复杂度 $O(k \cdot m^2)$。该层对哈希过滤后的候选对进行几何精化，实现细粒度实例识别。

**代码对应**：`SemanticMaterialGeometricDetector`（`semantic_material_geometric_detector.cpp`）、`buildSemanticHashKey`、`getMeshMaterialHash`/`getMeshMaterialIndex`（`material_matching.h`）、`computeMeshSimilarity`、`computeHausdorffDistance`、`hausdorffDistanceToSimilarity`。

---

### 3.2 哈希算法在多维度信息融合中的核心作用

在多维度相似性判别模型中，**哈希算法**承担着将异构信息转化为可比较键值的桥梁作用。传统实例化检测往往依赖单一维度（如几何ID或语义标签），难以兼顾效率与准确性。本节提出的哈希融合策略包括：（1）**语义哈希**——将语义属性编码为分组键，实现粗粒度预筛选；（2）**材质哈希**——将材质属性编码为哈希值，实现材质一致性过滤。二者与Hausdorff几何相似度形成"粗筛→细滤→精判"的三层递进结构。

**哈希融合的优势**：

1. **计算效率**：语义哈希可将 $n$ 个构件的候选对规模从 $O(n^2)$ 降至各组内 $O(n_i^2)$，其中 $\sum n_i = n$ 且 $n_i \ll n$；
2. **可配置性**：`semantic_hash_fields` 支持 category、family、type 的任意组合，`material_filter_mode` 支持 none/hash/index 三种模式；
3. **确定性**：相同输入产生相同哈希，保证重复运行结果一致；
4. **维度解耦**：各维度独立哈希，便于调试与参数调优。

---

### 3.3 基于语义哈希的候选集预筛选

#### 3.3.1 构件语义信息提取与语义哈希键生成

从RVT源数据或GLB元数据中提取构件语义信息，建立语义属性向量。配置项 `semantic_hash_fields` 指定参与分组的字段列表，支持 `category`、`family`、`type` 等字段的任意组合。

**语义哈希键生成**：通过 `buildSemanticHashKey` 将配置字段拼接为分组键。该键虽名为"哈希"，实则采用字符串拼接方式生成分组标识，其作用等价于语义维度的哈希映射——将语义空间划分为有限个等价类：

$$\text{Key}(m) = \text{concat}(c(m), f(m), t(m), ...)$$

其中 $c(m)$、$f(m)$、$t(m)$ 分别为 mesh $m$ 的 category、family、type 等字段；若某字段缺失则视为空字符串参与拼接。相同键的构件归入同一语义组，仅在组内进行后续材质过滤与几何比较。

**全局分组模式**：当 `semantic_hash_fields` 为空时，所有 mesh 归入 `__GLOBAL__` 单组，此时仅依赖材质过滤与几何相似度判定，适用于无语义元数据或需跨语义类别的实例检测场景。

#### 3.3.2 语义分组结果

语义分组后，得到初始分组集合 $\mathcal{G} = \{G_1, G_2, ..., G_k\}$，满足：

$$\bigcup_{i=1}^{k} G_i = M, \quad G_i \cap G_j = \emptyset \ (i \neq j)$$

仅在同组内进行后续材质过滤与几何相似度比较，从而将问题规模从 $n$ 降至各组内 $n_i \ll n$。

#### 3.3.3 基于材质哈希的材质过滤

在语义分组基础上，通过 `material_filter_mode` 控制是否按材质进一步过滤，避免材质差异大的构件被误判为实例。当模式为 `hash` 时，采用**材质哈希**实现高效过滤：

**材质哈希计算**（`getMeshMaterialHash`）：对 mesh 首个 primitive 的材质，将以下属性序列化为字符串后计算 `std::hash`：
- PBR 金属粗糙度：baseColorFactor、metallicFactor、roughnessFactor
- emissiveFactor、alphaMode、doubleSided

该哈希**不包含**纹理引用，仅比较材质因子，适用于BIM中大量使用程序化材质的场景。

**三种模式**：
- **none**：不按材质过滤，同语义组内所有 mesh 均可进入几何比较
- **hash**：仅当 `getMeshMaterialHash` 相同时才进入几何比较，实现材质维度的哈希等价类划分
- **index**：仅当 mesh 首个 primitive 的材质索引相同时才进入几何比较，适用于材质索引与语义强关联的场景

材质哈希过滤在语义分组与 Hausdorff 几何比较之间执行，进一步缩小候选对数量，避免几何相似但材质不同的构件（如同形状不同颜色的门）被误判为实例。

---

### 3.4 基于点云与 Hausdorff 距离的几何相似性判定

通过语义与材质过滤后的候选对，采用**点云采样 + 双向 Hausdorff 距离**进行几何相似度计算，判定是否满足实例化等价关系。

#### 3.4.1 点云提取与归一化

**步骤1：顶点提取** 从 mesh 的 POSITION 属性提取顶点集合 $V = \{v_1, v_2, ..., v_n\}$，支持多 primitive 的 mesh 合并所有顶点。

**步骤2：确定性下采样** 为控制计算量，使用 `hausdorff_max_sample_points` 对顶点进行确定性均匀下采样，得到点云 $P \subseteq V$，$|P| \leq N_{max}$。下采样采用等间隔索引选取，保证相同 mesh 在不同调用中产生一致结果。

**步骤3：包围盒归一化** 计算点云 AABB，将几何变换到归一化空间以消除平移与均匀缩放影响：

$$\mathbf{c} = \frac{\mathbf{b}_{min} + \mathbf{b}_{max}}{2}, \quad d_{max} = \max(\mathbf{b}_{max} - \mathbf{b}_{min})$$

$$\tilde{p} = \frac{p - \mathbf{c}}{d_{max}}$$

其中 $d_{max}$ 取包围盒三轴最大边长，避免退化几何导致除零。

#### 3.4.2 双向 Hausdorff 距离与相似度映射

**有向 Hausdorff 距离**：从点集 $A$ 到点集 $B$ 的有向距离定义为 $A$ 中每点到 $B$ 最近点距离的最大值：

$$h(A, B) = \max_{a \in A} \min_{b \in B} \|a - b\|$$

**双向 Hausdorff 距离**：

$$d_H(A, B) = \max(h(A, B), h(B, A))$$

实现中采用 KD-Tree 加速最近邻查询，将复杂度从 $O(|A| \cdot |B|)$ 降至 $O(|A| \log |B|)$。

**相似度映射**：将距离映射为 $[0, 1]$ 区间的相似度：

$$s = \frac{1}{1 + d_H}$$

当 $d_H = 0$ 时 $s = 1$（完全一致），$d_H$ 增大时 $s$ 单调递减。

#### 3.4.3 容差控制下的几何一致性判定

**判定条件**：设阈值 $\tau \in (0, 1]$（配置项 `similarity_threshold`），当 $s \geq \tau$ 时判定为几何等价：

$$m_1 \stackrel{g}{\sim} m_2 \iff s(m_1, m_2) \geq \tau$$

**容差判定的必要性**：实际BIM数据中存在导出精度差异、设计近似、参数化变体等微小几何差异。严格几何相等（$\tau = 1$）会导致实例化率极低；合理设定 $\tau$（如 0.9）可将"足够相似"的构件纳入同一实例组，显著提高实例化比例。容差阈值需平衡实例化率与视觉保真度。

**代码对应**：`hausdorff_similarity.cpp` 中的 `extractMeshPositions`、`normalizePointCloud`、`computeHausdorffDistance`、`hausdorffDistanceToSimilarity`、`computeMeshSimilarity`。

---

### 3.5 非均匀缩放实例的扩展支持

#### 3.5.1 变换矩阵分解与缩放类型识别

对构件变换矩阵 $\mathbf{M}$ 进行SVD分解：

$$\mathbf{M} = \mathbf{U} \mathbf{\Sigma} \mathbf{V}^T$$

提取缩放因子：

$$\mathbf{S} = \text{diag}(\sigma_1, \sigma_2, \sigma_3)$$

**缩放类型判定**：

$$\text{ScaleType} = \begin{cases} \text{Uniform} & \text{if } \frac{\max(\sigma_i) - \min(\sigma_i)}{\text{mean}(\sigma_i)} \leq 0.05 \\ \text{NonUniform} & \text{otherwise} \end{cases}$$

#### 3.5.2 非均匀缩放实例的表示方法

对于非均匀缩放实例，在gITF中使用 `EXT_mesh_gpu_instancing` 扩展，每个实例存储完整的 $4 \times 4$ 变换矩阵：

$$\mathbf{M}_{instance} = \begin{bmatrix} \mathbf{R}\mathbf{S} & \mathbf{t} \\ \mathbf{0}^T & 1 \end{bmatrix}$$

其中 $\mathbf{S}$ 可为非均匀缩放矩阵。

在几何比较阶段，mesh 的 POSITION 处于局部空间，点云归一化（3.4.1）基于包围盒进行尺度归一化；非均匀缩放会体现在归一化后的点云形状中，Hausdorff 距离可据此区分不同缩放类型的变体。`EXT_mesh_gpu_instancing` 支持每个实例存储完整的 $4 \times 4$ 变换矩阵，从而正确表示非均匀缩放实例。

---

### 3.6 实例化判定结果的数据结构

#### 3.6.1 实例组与非实例构件的混合表示

**实例组数据结构**（`InstancedMeshGroup`）：

```cpp
struct InstancedMeshGroup {
    int32_t representativeGltfModelIndex;      // 代表 mesh 所在 GLB 索引
    int32_t representativeMeshIndexInModel;   // 代表 mesh 在模型内索引
    std::string representativeMeshName;       // 代表 mesh 名称（含 glbStem|meshHashId）
    size_t meshSignature;                     // 几何/材质签名，用于去重
    std::vector<MeshInstanceInfo> instances;  // 实例变换列表
    std::vector<BoundingBox> representativePrimitiveBoundingBoxes;
    BoundingBox representativeMeshBoundingBox;
};
```

**非实例构件数据结构**（`NonInstancedMeshInfo`）：

```cpp
struct NonInstancedMeshInfo {
    int32_t originalGltfModelIndex;
    int32_t originalMeshIndexInModel;
    int32_t originalNodeIndexInModel;
    TransformComponents transform;
    int32_t sourceModelIndexInLoadedModels;   // 用于 per-GLB 统计
};
```

#### 3.6.2 语义映射保留与跨层级查找

为支持 Non-instanced LOD 与 HLOD 中的语义查找（如 RISCRVT 等外部系统），在导出 Non-instanced mesh 或合并叶节点时，mesh 名称采用 `glbStem|meshHashId` 格式：`glbStem` 为源 GLB 文件名（不含扩展名），`meshHashId` 为 mesh 的原始标识。`SemanticParser` 在 `getSemanticInfo(glbStem, meshHashId)` 中按此格式解析，实现跨层级、跨瓦片的语义一致性。

---

### 3.7 本章小结

本章针对BIM模型实例化检测问题，提出了**融合哈希算法的多维度信息的相似性判别模型**。主要贡献包括：

1. **形式化定义**了BIM构件的实例化等价关系，涵盖几何、语义、变换三个维度
2. **阐明了哈希算法在多维度融合中的核心作用**：语义哈希实现可配置的粗粒度分组降规模，材质哈希实现材质一致性过滤，二者与Hausdorff几何相似度形成"粗筛→细滤→精判"的三层递进结构
3. 设计了**语义-材质-几何三层筛选流程**：语义哈希预筛选（`buildSemanticHashKey`、`semantic_hash_fields`）降低问题规模；材质哈希过滤（`getMeshMaterialHash`、`material_filter_mode`）避免材质差异大的构件误判；Hausdorff 几何相似度实现细粒度判定
4. 提出了**点云采样 + 双向 Hausdorff 距离**的几何判定机制，通过归一化与相似度映射 $s = 1/(1+d_H)$ 实现容差控制下的实例识别
5. 扩展支持了**非均匀缩放实例**，增强了方法的适用性
6. 建立了**语义映射保留机制**（`glbStem|meshHashId`），为 Non-instanced LOD 与 HLOD 中的跨层级语义查找奠定数据基础

---

## 第四章 实例特征融合的BIM构件LOD分层轻量化方法

城市级BIM模型在Web端渲染时，除需实例化检测降低Draw Calls外，还需多级LOD与HLOD以支持按视距的渐进式加载。传统方法往往将实例化与LOD分离处理，或依赖跨层级的标识映射维护实例关系，难以兼顾空间划分的层次性与实例聚合的灵活性。本章在第三章多维度相似性判别模型的基础上，提出**实例特征融合的BIM构件LOD分层轻量化方法**。该方法将几何特征、语义特征与实例变换融合于统一数据结构（ExtendedMeshInfo），按 Family→Category→Abstract 逐级聚类实现 5 级 LOD；在 HLOD 构建中采用分层独立优化策略，每层级独立执行实例化检测与实例重建，实现跨瓦片聚合。本章首先定义实例特征融合的概念与HLOD总体框架，继而阐述空间划分、层级间实例重建与数据组织等关键技术。

### 4.1 实例特征融合的HLOD构建总体框架

#### 4.1.1 实例特征融合的定义与多级LOD分层实例特征

**定义4.1（实例特征融合）** 指将BIM构件的**几何特征**（AABB、体积、顶点数等）、**语义特征**（category、family、type）与**实例变换**（平移、旋转、缩放）融合于统一数据结构，支撑按层级逐步放宽相似性约束的LOD聚类。实例特征融合是5级LOD分层与HLOD分层独立优化的共同基础。

**定义4.2（多级LOD分层实例特征）** 指在HLOD的空间层级结构中，实例化信息在**纵向**（父子层级间）通过几何相似性传递，在**横向**（同级瓦片间）通过空间聚合实现统一表示：

- **纵向实例重建**：子层级的几何内容经简化后，在父层级通过重新检测（沿用第三章的融合哈希算法的多维度相似性判别模型）识别实例化机会
- **横向实例聚合**：跨越瓦片边界但几何相同的构件，在更高层级能够合并为统一的实例组

**形式化表达**：设HLOD层级为树结构 $\mathcal{T} = (N, E)$，节点 $n \in N$ 包含内容 $C(n)$。对于子节点 $n_{child}$ 中的实例组 $I_{child}$，其父节点通过简化-重建流程生成新的实例组：

$$I_{parent} = \text{InstancingDetect}(\text{Simplify}(\bigcup C(n_{child})))$$

实例化信息的传递不依赖标识继承，而是通过**几何一致性**在每一层级独立重新判定。其中 $\text{InstancingDetect}$ 采用第三章所述的融合哈希算法的多维度相似性判别模型。

#### 4.1.2 实例特征融合的数据结构载体

实例特征融合的载体为 **ExtendedMeshInfo** 结构体，其将几何、语义与实例三类特征统一存储：

- **几何特征**：AABB 包围盒、体积（volume）、对角线长度（diagonal）、顶点数（vertexCount），用于体积聚类、长宽比聚类与代表体选择；
- **语义特征**：SemanticInfo（category、family、type），用于 Family→Category→Abstract 的逐级语义聚类；
- **实例特征**：std::vector\<MeshInstanceInfo\> instances，存储各实例的变换组件（平移、旋转、缩放）。

在5级LOD生成中，`initializeLOD5` 从实例化检测结果与语义解析器中提取上述特征填充 ExtendedMeshInfo；LOD4 按 family 分组并辅以体积容差（5%）；LOD3 按 category 分组并辅以长宽比容差（20%）；LOD2 按抽象类别映射；LOD1 将实例变换融合为 Unit Cube 代理。各层级聚类均基于融合后的实例特征进行，实现从精细到概览的语义驱动LOD链。

#### 4.1.3 自底向上构建流程与分层独立优化策略

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    实例感知HLOD构建技术路线图                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   输入：原始BIM导出GLB文件集合（或 split 后的 non_instanced.glb / segmented 输出）│
│                              ↓                                           │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │           阶段1：空间层次划分（XZ平面四叉树，仅分割）              │   │
│   │  ┌─────────┐  ┌─────────┐  ┌─────────┐                         │   │
│   │  │Root Tile│  │Root Tile│  │Root Tile│  ← 覆盖全局范围(XZ)     │   │
│   │  │(Lv0)    │  │(Lv0)    │  │(Lv0)    │                         │   │
│   │  └────┬────┘  └────┬────┘  └────┬────┘                         │   │
│   │       └─────────────┼─────────────┘                             │   │
│   │              XZ平面四叉树分割（Y为高度）                         │   │
│   │       ┌─────────────┼─────────────┐                             │   │
│   │  ┌────┴────┐  ┌────┴────┐  ┌────┴────┐                         │   │
│   │  │Child L1│  │Child L1│  │Child L1│  ← 细分空间范围           │   │
│   │  │(Lv1)   │  │(Lv1)   │  │(Lv1)   │                             │   │
│   │  └────┬────┘  └────┬────┘  └────┬────┘                         │   │
│   │       └─────────────┼─────────────┘                             │   │
│   │          递归分割至满足终止条件（对象数/深度）                    │   │
│   │       ┌─────────────┼─────────────┐                             │   │
│   │  ┌────┴────┐  ┌────┴────┐  ┌────┴────┐                         │   │
│   │  │Leaf    │  │Leaf    │  │Leaf    │  ← 最细粒度空间单元       │   │
│   │  │(LvMax) │  │(LvMax) │  │(LvMax) │                             │   │
│   │  └─────────┘  └─────────┘  └─────────┘                         │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                              ↓                                           │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │           阶段2：自底向上内容生成（按层级从深到浅处理）           │   │
│   │                                                                │   │
│   │  Step 1: 叶子节点处理（LvMax → Lv0）                            │   │
│   │  ┌─────────────────────────────────────────────────────────┐   │   │
│   │  │ • 收集落入叶子空间范围的原始GLB文件                      │   │   │
│   │  │ • 直接合并GLB内容（保留原始几何，不做LOD简化）            │   │   │                
│   │  │ • 输出叶子节点GLB文件                                    │   │   │
│   │  └─────────────────────────────────────────────────────────┘   │   │
│   │                              ↓                                  │   │
│   │  Step 2: 父节点构建（分层独立优化）                             │   │
│   │  ┌─────────────────────────────────────────────────────────┐   │   │
│   │  │ ① 加载子节点GLB                                        │   │   │
│   │  │ ② 解包实例化扩展（unpackInstancing）                   │   │   │
│   │  │    将EXT_mesh_gpu_instancing展开为显式节点              │   │   │
│   │  │ ③ 合并所有子节点几何（mergeModel）                      │   │   │
│   │  │ ④ 网格简化（simplifyRatio=0.5）                        │   │   │
│   │  │ ⑤ 重新实例化判定（SemanticMaterialGeometricDetector）   │   │   │
│   │  │    - 对简化后的几何独立进行实例化检测                   │   │   │
│   │  │    - 不继承子层级实例组标识                              │   │   │
│   │  │ ⑥ 输出父节点GLB（含新识别的实例化）                     │   │   │
│   │  └─────────────────────────────────────────────────────────┘   │   │
│   │                              ↓                                  │   │
│   │  Step 3: 迭代直至根节点                                         │   │
│   │  （每层级独立进行简化-重建，形成分层LOD）                        │   │
│   │                                                                │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                              ↓                                           │
│   输出：自底向上构建的HLOD树 → 3D Tiles格式（tileset.json）             │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**分层独立优化策略核心思想**：

1. **空间分割与内容生成解耦**：先构建四叉树空间结构，再自底向上逐层生成内容
2. **层级独立实例化判定**：每层级独立调用`SemanticMaterialGeometricDetector`，不继承子层级的实例组标识
3. **简化-重建流程**：通过"解包→合并→简化→重新检测"四步，在父层级重新识别实例化机会
4. **几何相似性驱动**：实例化信息的纵向传递依赖简化后几何的一致性，而非标识映射

---

### 4.2 基于四叉树的空间层级划分

#### 4.2.1 空间分割策略与终止条件

**空间范围定义**：设BIM模型整体包围盒为 $\mathcal{B}_{world} = [x_{min}, x_{max}] \times [y_{min}, y_{max}] \times [z_{min}, z_{max}]$。

**四叉树分割规则**：在X-Z平面进行四叉树分割，Y方向保持完整（Y为高度轴）：

$$\text{Subdivide}(n) \rightarrow \{n_{bl}, n_{br}, n_{tl}, n_{tr}\}$$

子节点空间范围（Bottom-Left, Bottom-Right, Top-Left, Top-Right）：

$$\begin{aligned}
\mathcal{B}_{bl} &= [x_{min}, x_{mid}] \times [y_{min}, y_{max}] \times [z_{min}, z_{mid}] \\
\mathcal{B}_{br} &= [x_{mid}, x_{max}] \times [y_{min}, y_{max}] \times [z_{min}, z_{mid}] \\
\mathcal{B}_{tl} &= [x_{min}, x_{mid}] \times [y_{min}, y_{max}] \times [z_{mid}, z_{max}] \\
\mathcal{B}_{tr} &= [x_{mid}, x_{max}] \times [y_{min}, y_{max}] \times [z_{mid}, z_{max}]
\end{aligned}$$

**终止条件**（满足任一即停止分割）：

$$\text{Stop}(n) = \text{Depth}(n) \geq D_{max} \vee |C(n)| \leq N_{min}$$

其中：
- $D_{max}$：最大深度（配置项 `quadtree_max_depth`，默认 6 层）
- $N_{min}$：最小构件数（配置项 `quadtree_max_objects_per_tile`，默认 50 个）

**几何误差阈值分配**：设根节点几何误差为 $\epsilon_{root}$，子节点误差递减：

$$\epsilon(n_{child}) = \frac{\epsilon(n_{parent})}{2}$$

形成LOD层级：

| 层级 | 几何误差 | 内容精度 |
|------|----------|----------|
| LOD0 (Leaf) | $\epsilon_0$ | 完整细节 |
| LOD1 | $2\epsilon_0$ | 中等简化 |
| LOD2 | $4\epsilon_0$ | 高度简化 |
| LODn (Root) | $2^n\epsilon_0$ | 概览 |

#### 4.2.2 叶节点Tile的直接合并策略

叶节点是HLOD树的最深层节点，对应空间上最细粒度的分区。在自底向上构建策略中，叶节点采用**直接合并原始GLB**的方式生成内容：

**叶节点内容生成流程**：

1. **收集原始GLB文件**：遍历所有输入的原始GLB文件，根据构件中心点坐标判断其是否落入叶节点空间范围

$$\text{Objects}(n_{leaf}) = \{obj \in \text{InputGLBs} \ | \ obj.center \in \mathcal{B}(n_{leaf})\}$$

2. **直接合并GLB内容**：使用`mergeModel`函数将落入该叶节点的所有原始GLB文件合并为单一模型
   - 合并时保持原始几何数据不变
   - 不调用网格简化算法
   - 不额外进行实例化判定
   - **语义映射保留**：合并时为 mesh 名称添加 `sourceStem|` 前缀（`sourceStem` 为源 GLB 文件名），形成 `glbStem|meshHashId` 格式，便于 `SemanticParser` 在父层级或外部系统（如 RISCRVT）中按源文件与 mesh 标识进行语义查找

3. **输出叶节点GLB**：将合并后的模型写入文件，命名为 `T{level}_{x}_{y}.glb`

**叶节点内容特点**：

```
LeafTile Content (T{L}_{X}_{Y}.glb):
├── 原始GLB几何数据（完整保留）
│   ├── Mesh A (来自原始文件1)
│   ├── Mesh B (来自原始文件2)
│   └── ...
└── 无LOD变体（仅单一精度）
    └── 注：LOD将在父层级通过简化-重建生成
```

**设计 rationale**：

- **简化延迟**：叶节点作为最精细层级，直接保留原始数据，避免在大量叶节点上进行重复简化计算
- **实例化延迟**：跨原始GLB文件的重复构件，在叶节点层级保持独立，在父层级合并简化后通过重新检测识别为实例
- **计算效率**：叶节点数量众多（四叉树叶子节点数为 $O(4^h)$），直接合并比逐层简化更高效

---

### 4.3 层级间实例重建与再识别机制

#### 4.3.1 子节点解包与几何聚合

在父节点构建阶段，首先加载所有子节点的GLB文件，将其几何内容合并为单一模型。

**步骤1：加载子节点GLB**

对父节点的每个子节点 $n_{child}$，加载其GLB文件：

$$\text{Models}_{children} = \{\text{LoadGLB}(n_{child}.tileFilename) \ | \ n_{child} \in \text{Children}(n_{parent})\}$$

**步骤2：解包实例化扩展（unpackInstancing）**

若子节点GLB中包含 `EXT_mesh_gpu_instancing` 扩展，需将其展开为显式节点，以便后续简化和重新检测：

$$\text{Unpacked}(M) = \text{ConvertInstancesToNodes}(M)$$

展开操作将每个实例从GPU实例化表示转换为独立的几何节点，保留变换信息：

- 从扩展中读取 `TRANSLATION`、`ROTATION`、`SCALE` 属性
- 为每个实例创建一个独立节点，应用相应变换
- 移除 `EXT_mesh_gpu_instancing` 扩展

**步骤3：合并几何**

使用 `mergeModel` 将所有解包后的模型合并：

$$G_{parent}^{raw} = \text{Merge}(\{\text{Unpacked}(M) \ | \ M \in \text{Models}_{children}\})$$

合并过程处理缓冲区、访问器、网格、材质等元素的索引偏移，确保所有几何数据正确聚合到单一模型中。

#### 4.3.2 网格简化与再实例化判定

**步骤4：网格简化**

对聚合后的几何模型应用简化算法，降低面数以生成父层级的LOD表示：

$$G_{parent}^{simplified} = \text{Simplify}(G_{parent}^{raw}, \text{target_ratio}=0.5)$$

简化参数：
- `target_ratio`：目标面数比例，默认为 50%（即保留约一半三角形）
- `minSimplifyIndexCount`：最小简化索引数，低于此值的 mesh 不参与简化（默认 300）

**步骤5：重新实例化判定**

对简化后的几何，**独立**执行实例化检测流程，不继承子层级的实例组标识：

$$\text{DetectionResult} = \text{SemanticMaterialGeometricDetector}(G_{parent}^{simplified})$$

判定流程与第三章相同，沿用同一套参数：
1. **语义预筛选**：`semantic_hash_fields` 可配置（如 category、family、type），空则 `__GLOBAL__`
2. **材质过滤**：`material_filter_mode`（none / hash / index）
3. **Hausdorff 几何相似度**：点云采样 + 双向 Hausdorff，$s = 1/(1+d_H)$，$s \geq \tau$ 判定为实例
4. 识别新的实例组与非实例构件

子节点 GLB 中 mesh 若含 `glbStem|meshHashId` 格式名称，`SemanticParser` 可据此解析语义信息，支持跨层级的语义一致性判定。

**分层独立优化特性**：

不同于"实例保持"策略（维护标识映射），本方法采用"**实例重建**"策略：
- 每层级独立进行实例化判定
- 子层级的实例组标识不在父层级复用
- 父层级基于简化后的几何重新识别实例化机会

这种策略的优势：
- **自适应LOD**：不同层级可根据简化后的几何特征识别新的实例化模式
- **误差容限**：简化导致的微小几何变化不会破坏实例化关系（通过容差判定适应）
- **计算独立**：各层级实例化判定解耦，便于并行化

**实例特征融合在5级LOD中的体现**：在实例化LOD（非HLOD）阶段，`ExtendedMeshInfo` 结构体承载实例特征融合——几何特征（aabb、volume、diagonal、vertexCount）、语义特征（semantic）与实例变换（instances）统一存储。LOD5→LOD1 按 Family→Category→Abstract 逐级聚类，每级聚类均基于融合后的实例特征进行代表体选择与实例合并，实现从精细到概览的语义驱动LOD链。

#### 4.3.3 分层独立优化的实例化效果

由于采用**分层独立优化**策略，父子层级间不存在显式的实例标识映射。实例化效果的传递通过几何相似性隐式实现：

**实例化传递的三种情形**：

| 子层级状态 | 简化后几何 | 父层级检测结果 | 效果描述 |
|------------|------------|----------------|----------|
| 多个相同实例 | 简化后仍相似 | 识别为新实例组 | 实例化关系延续 |
| 多个相似构件 | 简化后趋同 | 识别为新实例组 | 获得新的实例化机会 |
| 不同构件 | 简化后仍不同 | 保持非实例化 | 保持独立网格 |

**关键特性**：

1. **无标识继承**：父层级完全基于简化后的几何重新判定，不查询子层级的实例组信息
2. **容差自适应**：简化导致的轻微几何变形在容差范围内，大概率仍被识别为同一实例组
3. **层级特异性**：不同层级可能识别出不同的实例化模式（如高层级识别更大的聚合组）

**代码实现对应**：

```cpp
// 父节点构建流程（对应代码实现）
CesiumGltf::Model mergedModel;
for (auto& child : node->children) {
    auto model = LoadGLB(child->tileFilename);
    unpackInstancing(model);           // 解包实例化
    mergeModel(mergedModel, model);    // 合并几何
}

auto simplified = simplifyModel(mergedModel, 0.5);  // 网格简化

// 独立进行实例化判定（不传入子层级实例信息，沿用 material_filter_mode、semantic_hash_fields 等参数）
SemanticMaterialGeometricDetector detector(semanticParser, semanticHashFields, similarityThreshold, limit, hausdorffMaxSamplePoints, allowUnknownCrossMeshClustering, materialFilterMode);
auto result = detector.detect({simplified});  // 重新检测

writeInstancedGlb(simplified, result, outputPath);  // 输出结果
```

---

### 4.4 跨瓦片实例聚合与数据组织

#### 4.4.1 瓦片边界处的实例分割问题

**问题描述**：几何相同的构件因跨越瓦片边界被分割到不同子节点，导致：

1. 各子节点需分别存储该实例组的部分实例
2. 父节点若不处理，将丢失实例化优势
3. 渲染时跨瓦片边界可能出现视觉不一致

**形式化描述**：设全局实例组 $I$ 的实例集合为 $\{inst_1, inst_2, ..., inst_n\}$，空间分布跨越多个叶节点：

$$\text{Distrib}(I) = \{n \in \text{Leaves} \ | \ \exists inst \in I: \text{Pos}(inst) \in \mathcal{B}(n)\}$$

若 $|\text{Distrib}(I)| > 1$，则该实例组在叶节点层级被分割。

#### 4.4.2 分层独立优化中的跨瓦片实例识别

在分层独立优化策略下，跨瓦片实例的识别不依赖全局注册表，而是通过父层级的**简化-重建**流程自然实现：

**跨瓦片实例识别机制**：

1. **子节点展开**：各子节点的实例化扩展被解包为独立几何节点
2. **跨子节点合并**：所有子节点的几何（包括来自不同子瓦片的相同构件）被合并到单一模型
3. **统一简化**：合并后的几何作为一个整体进行网格简化
4. **全局检测**：对简化后的几何执行实例化检测，此时跨子瓦片的相同构件处于同一检测空间

**效果**：若子瓦片A和子瓦片B各包含同一实例组的部分实例，在父层级：
- 这些实例被展开为独立几何
- 经简化后几何特征趋于一致
- 在重新检测时因几何相似性被识别为同一实例组
- 从而实现跨瓦片实例的聚合

#### 4.4.3 父层级跨瓦片实例统一表示

**分层独立优化的聚合效果**：

父层级的实例化检测结果自然包含跨子瓦片的实例聚合：

$$I_{parent} = \text{InstancingDetect}(\text{Simplify}(\bigcup_{i} \text{Unpack}(n_i)))$$

其中跨瓦片实例的变换矩阵被统一收集：

$$\text{Instances}(I_{parent}) = \{T_j \ | \ T_j \in \text{TransformsOf}(I_{source}, \text{AllChildren})\}$$

这种聚合方式的优势：
- **无全局状态依赖**：不需要维护跨层级的实例标识映射
- **自适应精度**：简化-检测流程容忍瓦片边界的微小几何差异
- **自然扩展**：新增瓦片无需更新全局注册表

**数据组织优化**：

```
ParentTile Content:
├── Cross-Tile Aggregated Instances
│   ├── InstanceGroup A (merged from Leaf1, Leaf2, Leaf3):
│   │   ├── Instance from Leaf1: [transform1, transform2]
│   │   ├── Instance from Leaf2: [transform3]
│   │   └── Instance from Leaf3: [transform4, transform5]
│   └── InstanceGroup B (merged from Leaf2, Leaf4)
├── Local Instances (within single child)
│   └── ...
└── Non-Instanced Geometry
    └── ...
```

---

### 4.5 实例与非实例混合的数据组织

#### 4.5.1 混合HLOD节点数据组织

每个 HLOD 瓦片对应单一 GLB 文件（如 `T{level}_{x}_{y}.glb`），其内容为**实例化与非实例化几何的混合**：实例化 mesh 使用 `EXT_mesh_gpu_instancing` 扩展存储，非实例化 mesh 以传统节点形式存储，二者共存于同一 GLB 中。

**内存与渲染效率分析**：

设节点包含 $n$ 个实例组，第 $i$ 组有 $k_i$ 个实例，几何复杂度为 $t_i$ 三角形。

- **纯实例化渲染代价**：$O(n)$ 绘制调用
- **完全展开渲染代价**：$O(\sum k_i)$ 绘制调用
- **存储节省率**：

$$\eta = 1 - \frac{\text{Size}_{instanced}}{\text{Size}_{expanded}} = 1 - \frac{\sum t_i + c \cdot \sum k_i}{\sum (t_i \cdot k_i)}$$

其中 $c$ 为每个实例的变换数据大小（通常16-24字节）。

#### 4.5.2 面向3D Tiles的层级输出格式

**3D Tiles 规范映射**：每节点 `content.uri` 指向单一 GLB 文件，该文件内实例化与非实例化内容混合存储。tileset 结构示例：

```json
{
  "asset": {"version": "1.1"},
  "geometricError": 100.0,
  "root": {
    "boundingVolume": {"region": [...]},
    "geometricError": 50.0,
    "refine": "REPLACE",
    "content": {"uri": "T0_0_0.glb"},
    "children": [
      {
        "boundingVolume": {"region": [...]},
        "geometricError": 25.0,
        "content": {"uri": "T1_0_0.glb"}
      }
    ]
  }
}
```

**GLB内容组织**：实例化 mesh 使用 `EXT_mesh_gpu_instancing` 扩展存储 TRANSLATION、ROTATION、SCALE 属性；非实例化 mesh 以常规节点+mesh 形式存储。

---

### 4.6 本章小结

本章针对复杂BIM模型的层次化组织需求，提出了**实例特征融合的BIM构件LOD分层轻量化方法**。主要贡献包括：

1. **形式化定义**了"实例特征融合"与"多级LOD分层实例特征"概念，明确几何、语义、实例变换的融合方式，以及纵向重建（通过几何相似性）与横向聚合（通过空间合并）两个核心维度
2. **实例特征融合的5级LOD**：将几何特征、语义特征与实例变换融合于 ExtendedMeshInfo，按 Family→Category→Abstract 逐级聚类，实现语义驱动的LOD分层
3. 设计了**自底向上的四叉树空间分割与内容生成策略**：先进行XZ平面四叉树空间划分，再从叶子节点开始逐层向上生成LOD内容
4. 提出了**分层独立优化机制**：叶子节点直接合并原始GLB数据，父节点通过"解包→合并→简化→重新检测"四步流程独立进行实例化判定，每层级沿用第三章的融合哈希算法的多维度相似性判别模型
5. 实现了**层级间实例重建策略**：不同于标识继承方式，本方法通过简化-重建流程在每一层级独立识别实例化机会，依赖几何一致性而非标识映射实现实例化信息的纵向传递
6. 设计了**实例与非实例混合的数据组织格式**，无缝映射到3D Tiles标准，兼顾渲染效率与规范兼容性

该方法与第三章形成完整技术链条：第三章的融合哈希算法的多维度相似性判别模型提供单一层级的实例化检测能力，本章通过实例特征融合与分层独立优化策略将其应用于5级LOD与HLOD的多层级构建中，共同支撑复杂BIM模型的高效轻量化与可视化。

---

## 附：公式符号对照表

| 符号 | 含义 |
|------|------|
|$m, m_i$ | BIM构件 |
|$M$ | 构件集合 |
|$V$ | 顶点集合 |
|$S$ | 几何表面 |
|$d_H$ | Hausdorff距离 |
|$\epsilon_g, \epsilon_v, \epsilon_n$ | 几何/顶点/法向量容差 |
|$\mathbf{M}, \mathbf{T}, \mathbf{R}, \mathbf{S}$ | 变换/平移/旋转/缩放矩阵 |
|$\mathcal{B}$ | 包围盒 |
|$\mathcal{H}$ | 哈希表 |
|$\mathcal{I}$ | 实例集合/注册表 |
|$n, n_i$ | HLOD节点 |
|$\mathcal{T}$ | HLOD树结构 |
|$I, I_i$ | 实例组 |
|$\epsilon_{geom}$ | 几何误差阈值 |
