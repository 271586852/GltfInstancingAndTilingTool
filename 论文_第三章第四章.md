# 顾及多级LOD分层实例特征的复杂BIM模型轻量化方法

## 第三章 融合哈希的语义几何联合实例化判定方法

### 3.1 实例化判定的形式化定义与流程框架

#### 3.1.1 BIM构件等价关系定义

在BIM模型中，实例化检测的本质是识别**几何等价**且**语义相容**的构件集合。为此，首先建立构件等价关系的形式化定义。

**定义3.1（几何等价）** 设两个BIM构件 $m_1$ 和 $m_2$ 的几何表面分别为 $S_1$ 和 $S_2$，若存在刚体变换 $T \in SE(3)$（包含旋转和平移），使得两个表面在Hausdorff距离意义下满足容差约束：

$$d_H(S_1, T(S_2)) = \max\left\{\sup_{p \in S_1} \inf_{q \in S_2} \|p - T(q)\|, \sup_{q \in S_2} \inf_{p \in S_1} \|T^{-1}(p) - q\|\right\} \leq \epsilon_g$$

则称 $m_1$ 和 $m_2$ 几何等价，记作 $m_1 \stackrel{g}{\sim} m_2$，其中 $\epsilon_g$ 为几何容差阈值。

**定义3.2（语义等价）** 设构件 $m$ 的语义属性为五元组 $S(m) = (C, F, T, L, M)$，其中：
- $C$：构件类别（Category），如墙、门、窗
- $F$：族名称（Family）
- $T$：类型名称（Type）
- $L$：层级信息（Level）
- $M$：材质信息（Material）

若两个构件 $m_1, m_2$ 满足：

$$C(m_1) = C(m_2) \wedge F(m_1) = F(m_2) \wedge T(m_1) = T(m_2)$$

则称二者语义等价，记作 $m_1 \stackrel{s}{\sim} m_2$。

**定义3.3（变换等价）** 设构件 $m_1, m_2$ 的局部到世界坐标变换矩阵分别为 $\mathbf{M}_1, \mathbf{M}_2 \in \mathbb{R}^{4 \times 4}$。对变换矩阵进行分解：

$$\mathbf{M} = \mathbf{T} \cdot \mathbf{R} \cdot \mathbf{S}$$

其中 $\mathbf{T}$ 为平移，$\mathbf{R}$ 为旋转，$\mathbf{S} = \text{diag}(s_x, s_y, s_z)$ 为缩放。若满足：

$$\|s_x - s_y\| + \|s_y - s_z\| + \|s_z - s_x\| \leq \delta_{non-uni}$$

则称缩放类型为**均匀缩放**；否则为**非均匀缩放**。变换等价要求缩放类型一致且比例因子在容差范围内。

**定义3.4（实例化等价）** 综合上述定义，构件 $m_1, m_2$ 可实例化的充要条件为：

$$m_1 \stackrel{inst}{\sim} m_2 \iff m_1 \stackrel{g}{\sim} m_2 \wedge m_1 \stackrel{s}{\sim} m_2 \wedge \text{TransformCompatible}(m_1, m_2)$$

#### 3.1.2 语义-几何双层判定流程

基于上述定义，提出**语义-几何双层筛选**的判定流程：

```
┌─────────────────────────────────────────────────────────────────────┐
│                    实例化判定总体流程                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐          │
│  │ 输入BIM构件  │───→│  语义预筛选   │───→│ Hausdorff 几何相似度 │   │
│  │   集合 M    │    │  (粗粒度)     │    │  (细粒度)    │          │
│  └──────────────┘    └──────────────┘    └──────────────┘          │
│                               │                  │                 │
│                               ↓                  ↓                 │
│                        ┌──────────────┐    ┌──────────────┐       │
│                        │ 语义分组集合 │    │ 点云采样+Hausdorff │  │
│                        │   G₁,G₂...  │    │              │       │
│                        └──────────────┘    └──────┬───────┘       │
│                                                    │               │
│                                                    ↓               │
│                                           ┌──────────────┐        │
│                                           │  相似度精化判定 │       │
│                                           │              │        │
│                                           └──────┬───────┘        │
│                                                  │                 │
│                                                  ↓                 │
│                                           ┌──────────────┐        │
│                                           │  实例化分组   │        │
│                                           │   I₁,I₂...   │        │
│                                           └──────────────┘        │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

**第一层：语义预筛选** 时间复杂度 $O(n \log n)$，通过语义属性快速分组，将问题规模从 $n$ 降至各组内规模 $n_i \ll n$。

**第二层：Hausdorff 几何相似度判定** 时间复杂度 $O(n_i)$，通过点云采样与双向 Hausdorff 距离计算相似度 $s = 1/(1+d_H)$，识别潜在实例候选。

**第三层：相似度精化判定** 对组内候选进行 Hausdorff 相似度比对，$s \geq \tau$ 时判定为几何等价，时间复杂度 $O(k \cdot m^2)$，其中 $k$ 为语义组数，$m$ 为组内平均规模。

**代码对应**：`SemanticMaterialGeometricDetector`（`semantic_material_geometric_detector.cpp`）、`buildSemanticHashKey`、`computeMeshSimilarity`、`hausdorff_similarity.cpp` 中的 `computeHausdorffDistance`、`hausdorffDistanceToSimilarity`。

---

### 3.2 基于语义约束的候选集预筛选

#### 3.2.1 构件语义信息提取

从RVT源数据中提取构件语义信息，建立语义属性向量：

$$\mathbf{s}_i = [c_i, f_i, t_i, l_i, m_i]^T$$

其中各分量编码规则：
- $c_i \in \mathbb{N}$：构件类别的枚举编码
- $f_i \in \Sigma^*$：族名称的字符串哈希值
- $t_i \in \Sigma^*$：类型名称的字符串哈希值
- $l_i \in \mathbb{N}$：楼层编号
- $m_i \in \mathbb{N}$：材质ID

**语义冲突消解**：对于族名称相同但几何差异显著的构件（如参数化族的不同变体），引入几何指纹作为二级校验：

$$\text{ValidGroup}(G) = \forall m_i, m_j \in G: \text{VolRatio}(m_i, m_j) \in [\tau_{min}, \tau_{max}]$$

其中体积比容差 $[\tau_{min}, \tau_{max}] = [0.8, 1.25]$ 用于排除几何差异过大的语义同族构件。

#### 3.2.2 语义一致性约束规则与候选分组

建立语义分组规则集合 $\mathcal{R} = \{R_1, R_2, R_3\}$：

**规则R1（严格匹配）**：适用于标准构件（门窗、结构柱等）

$$m_i \stackrel{R_1}{\sim} m_j \iff c_i = c_j \wedge f_i = f_j \wedge t_i = t_j \wedge |l_i - l_j| \leq 1$$

**规则R2（跨层匹配）**：适用于重复楼层中的相同构件

$$m_i \stackrel{R_2}{\sim} m_j \iff c_i = c_j \wedge f_i = f_j \wedge t_i = t_j$$

**规则R3（材质放宽）**：适用于仅材质不同的同类构件

$$m_i \stackrel{R_3}{\sim} m_j \iff c_i = c_j \wedge f_i = f_j \wedge \text{GeoSim}(m_i, m_j) \geq 0.95$$

语义分组后，得到初始分组集合 $\mathcal{G} = \{G_1, G_2, ..., G_k\}$，满足：

$$\bigcup_{i=1}^{k} G_i = M, \quad G_i \cap G_j = \emptyset \ (i \neq j)$$

---

### 3.3 基于归一化与哈希的几何相似性判定

#### 3.3.1 包围盒尺度归一化与坐标系对齐

对语义分组内的构件进行几何归一化，消除平移、旋转、均匀缩放的影响。

**步骤1：轴对齐包围盒计算**

对构件几何顶点集合 $V = \{v_1, v_2, ..., v_n\}$，计算AABB：

$$\mathbf{b}_{min} = \min_{v \in V} v, \quad \mathbf{b}_{max} = \max_{v \in V} v$$

包围盒中心与尺寸：

$$\mathbf{c} = \frac{\mathbf{b}_{min} + \mathbf{b}_{max}}{2}, \quad \mathbf{d} = \mathbf{b}_{max} - \mathbf{b}_{min}$$

**步骤2：尺度归一化**

将几何变换到单位立方体 $[-0.5, 0.5]^3$：

$$\tilde{v} = \frac{v - \mathbf{c}}{d_{max}}, \quad d_{max} = \max(d_x, d_y, d_z)$$

**步骤3：主成分分析（PCA）对齐**（可选）

对非对称几何，计算协方差矩阵：

$$\mathbf{\Sigma} = \frac{1}{n} \sum_{i=1}^{n} (\tilde{v}_i - \bar{v})(\tilde{v}_i - \bar{v})^T$$

对 $\mathbf{\Sigma}$ 进行特征分解 $\mathbf{\Sigma} = \mathbf{Q}\mathbf{\Lambda}\mathbf{Q}^T$，利用特征向量 $\mathbf{Q}$ 构造旋转矩阵，使几何主方向与坐标轴对齐。

归一化后的几何表示为：

$$\hat{V} = \{\mathbf{R}_{PCA} \cdot \tilde{v}_i \ | \ v_i \in V\}$$

#### 3.3.2 几何签名生成与哈希索引构建

**几何签名设计**：基于归一化几何的多级特征描述：

**L1-表面特征**：顶点位置与法向量统计

$$\mathbf{f}_{surf} = [\mu_p, \sigma_p, \mu_n, \sigma_n, N_v, N_f]$$

其中 $\mu_p, \sigma_p$ 为顶点位置均值与标准差，$\mu_n, \sigma_n$ 为法向量统计，$N_v, N_f$ 为顶点数和面数。

**L2-拓扑特征**：边与面的连接关系

$$\mathbf{f}_{topo} = [E_{total}, E_{boundary}, F_{tri}, F_{quad}, H_{Euler}]$$

其中 $H_{Euler} = V - E + F$ 为欧拉特征数。

**L3-几何哈希**：基于离散化的空间占用签名

将归一化空间划分为 $8 \times 8 \times 8$ 的体素网格，计算每个体素的几何占用：

$$\mathbf{h}[i,j,k] = \begin{cases} 1 & \text{if } \exists f \in F: f \cap \text{Voxel}[i,j,k] \neq \emptyset \\ 0 & \text{otherwise} \end{cases}$$

最终几何签名为特征级联：

$$\mathbf{Sig}(m) = [\mathbf{f}_{surf} \ | \ \mathbf{f}_{topo} \ | \ \text{hash}(\mathbf{h})]$$

**哈希函数选择**：

采用64位FNV-1a哈希，对浮点特征进行离散化后计算：

$$\text{Hash}(\mathbf{Sig}) = \bigoplus_{i=1}^{k} (\text{hash}_i \times 1099511628211) \mod 2^{64}$$

其中 $\text{hash}_i$ 为各特征分量离散化后的整数值。

建立哈希表索引：

$$\mathcal{H}: \text{Hash} \rightarrow \{m \in M \ | \ \text{Hash}(\mathbf{Sig}(m)) = \text{Hash}\}$$

#### 3.3.3 容差控制下的几何一致性判定机制

在BIM模型中，"完全相同"的构件极少存在，但"足够相似"的构件大量存在。容差机制的核心作用是**扩大实例化匹配范围**，将几何相似但非严格一致的构件纳入同一实例组，从而显著提高实例化比例。

**容差判定的必要性**：

实际BIM数据中存在多种微小差异：
- 导出精度差异：顶点坐标浮点误差（如0.0001m级偏差）
- 设计近似：同一规格构件的尺寸微调（如柱子高度差1mm）
- 参数化变体：同族构件的缩放比例差异（如0.999 vs 1.001）

严格几何相等（容差=0）会将这些构件判定为不同实例，导致实例化率极低。容差机制通过设定合理阈值，**将这些"长得像"的构件判定为实例**。

**顶点级容差判定**：

设两个归一化几何的顶点集 $\hat{V}_1, \hat{V}_2$，建立对应关系后，顶点偏差：

$$\delta_v = \max_{(\hat{v}_i, \hat{v}_j) \in \mathcal{C}} \|\hat{v}_i - \hat{v}_j\|$$

其中 $\mathcal{C}$ 为最近点对应集合。

**法向量容差判定**：

$$\delta_n = \max_{(n_i, n_j) \in \mathcal{C}_n} \arccos(n_i \cdot n_j)$$

**综合判定条件（容差宽松化）**：

$$m_1 \stackrel{g}{\sim} m_2 \iff \delta_v \leq \epsilon_v \wedge \delta_n \leq \epsilon_n$$

其中默认容差 $\epsilon_v = 0.001$（归一化单位），$\epsilon_n = 5°$。

**容差对实例化率的影响**：

| 容差策略 | 判定标准 | 实例化率 | 适用场景 |
|---------|---------|---------|---------|
| 严格匹配（容差=0） | 几何完全一致 | 低（~20%） | 精确匹配需求 |
| **容差匹配** | 几何相似即可 | **高（~70%）** | **通用BIM轻量化** |

**注**：容差阈值需平衡实例化率与视觉保真度，过大的容差可能导致视觉差异明显的构件被合并。

---

### 3.4 非均匀缩放实例的扩展支持

#### 3.4.1 变换矩阵分解与缩放类型识别

对构件变换矩阵 $\mathbf{M}$ 进行SVD分解：

$$\mathbf{M} = \mathbf{U} \mathbf{\Sigma} \mathbf{V}^T$$

提取缩放因子：

$$\mathbf{S} = \text{diag}(\sigma_1, \sigma_2, \sigma_3)$$

**缩放类型判定**：

$$\text{ScaleType} = \begin{cases} \text{Uniform} & \text{if } \frac{\max(\sigma_i) - \min(\sigma_i)}{\text{mean}(\sigma_i)} \leq 0.05 \\ \text{NonUniform} & \text{otherwise} \end{cases}$$

#### 3.4.2 非均匀缩放实例的表示方法

对于非均匀缩放实例，在gITF中使用 `EXT_mesh_gpu_instancing` 扩展，每个实例存储完整的 $4 \times 4$ 变换矩阵：

$$\mathbf{M}_{instance} = \begin{bmatrix} \mathbf{R}\mathbf{S} & \mathbf{t} \\ \mathbf{0}^T & 1 \end{bmatrix}$$

其中 $\mathbf{S}$ 可为非均匀缩放矩阵。

在几何归一化阶段，对非均匀缩放构件进行**逆变换归一化**：

$$\hat{V}_{norm} = \mathbf{S}^{-1} \cdot \mathbf{R}^{-1} \cdot (V - \mathbf{t})$$

确保几何签名在**去缩放**空间计算，保证非均匀缩放变体也能被识别为同一实例组。

---

### 3.5 实例化判定结果的数据结构

#### 3.5.1 实例组与非实例构件的混合表示

**实例组数据结构**：

```cpp
struct InstancedMeshGroup {
    // 全局唯一实例组ID
    InstanceGroupID globalId;

    // 共享几何数据（归一化后）
    NormalizedGeometry baseGeometry;

    // 实例列表
    std::vector<InstanceTransform> instances;

    // 语义属性
    SemanticAttributes sharedSemantics;

    // 缩放类型
    ScaleType scaleType;
};
```

**非实例构件数据结构**：

```cpp
struct NonInstancedMeshInfo {
    MeshID id;
    WorldGeometry geometry;
    SemanticAttributes semantics;
    TransformMatrix transform;
};
```

#### 3.5.2 全局实例标识与空间索引建立

为支持第四章的跨瓦片/跨层级实例保持，建立**全局实例注册表**：

$$\mathcal{I}_{global}: \text{InstanceGroupID} \rightarrow \text{InstancedMeshGroup}$$

同时建立实例组到原始构件的空间索引：

$$\mathcal{S}: \text{BoundingBox} \rightarrow \{\text{InstanceGroupID}\}$$

使用R*树或四叉树实现，支持范围查询：

$$\text{Query}(bbox) = \{I \ | \ \text{BBox}(I) \cap bbox \neq \emptyset\}$$

---

### 3.6 本章小结

本章针对BIM模型实例化检测问题，提出了**融合语义约束与几何哈希的联合判定方法**。主要贡献包括：

1. **形式化定义**了BIM构件的实例化等价关系，涵盖几何、语义、变换三个维度
2. 设计了**语义-几何双层筛选流程**，通过语义预筛选降低问题规模，通过哈希索引实现快速几何匹配
3. 提出了**归一化-哈希-容差精化**的三级几何判定机制，在保证精度的同时提升效率
4. 扩展支持了**非均匀缩放实例**，增强了方法的适用性
5. 建立了**全局实例标识体系**，为后续HLOD构建中的实例保持奠定了数据基础

---

## 第四章 顾及多级LOD分层实例特征的HLOD构建方法

### 4.1 实例感知HLOD构建总体框架

#### 4.1.1 多级LOD分层实例特征的定义

**定义4.1（多级LOD分层实例特征）** 指在HLOD的空间层级结构中，实例化信息在**纵向**（父子层级间）通过几何相似性传递，在**横向**（同级瓦片间）通过空间聚合实现统一表示：

- **纵向实例重建**：子层级的几何内容经简化后，在父层级通过重新检测识别实例化机会
- **横向实例聚合**：跨越瓦片边界但几何相同的构件，在更高层级能够合并为统一的实例组

**形式化表达**：设HLOD层级为树结构 $\mathcal{T} = (N, E)$，节点 $n \in N$ 包含内容 $C(n)$。对于子节点 $n_{child}$ 中的实例组 $I_{child}$，其父节点通过简化-重建流程生成新的实例组：

$$I_{parent} = \text{InstancingDetect}(\text{Simplify}(\bigcup C(n_{child})))$$

实例化信息的传递不依赖标识继承，而是通过**几何一致性**在每一层级独立重新判定。

#### 4.1.2 自底向上构建流程与分层独立优化策略

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    实例感知HLOD构建技术路线图                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   输入：原始BIM导出GLB文件集合                                          │
│                              ↓                                           │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │           阶段1：空间层次划分（XZ平面四叉树，仅分割）              │   │
│   │  ┌─────────┐  ┌─────────┐  ┌─────────┐                         │   │
│   │  │Root Tile│  │Root Tile│  │Root Tile│  ← 覆盖全局范围(XZ)     │   │
│   │  │(Lv0)    │  │(Lv0)    │  │(Lv0)    │                         │   │
│   │  └────┬────┘  └────┬────┘  └────┬────┘                         │   │
│   │       └─────────────┼─────────────┘                             │   │
│   │              XZ平面四叉树分割（Y为高度）                         │   │
│   │       ┌─────────────┼─────────────┐                             │   │
│   │  ┌────┴────┐  ┌────┴────┐  ┌────┴────┐                         │   │
│   │  │Child L1│  │Child L1│  │Child L1│  ← 细分空间范围           │   │
│   │  │(Lv1)   │  │(Lv1)   │  │(Lv1)   │                             │   │
│   │  └────┬────┘  └────┬────┘  └────┬────┘                         │   │
│   │       └─────────────┼─────────────┘                             │   │
│   │          递归分割至满足终止条件（对象数/深度）                    │   │
│   │       ┌─────────────┼─────────────┐                             │   │
│   │  ┌────┴────┐  ┌────┴────┐  ┌────┴────┐                         │   │
│   │  │Leaf    │  │Leaf    │  │Leaf    │  ← 最细粒度空间单元       │   │
│   │  │(LvMax) │  │(LvMax) │  │(LvMax) │                             │   │
│   │  └─────────┘  └─────────┘  └─────────┘                         │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                              ↓                                           │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │           阶段2：自底向上内容生成（按层级从深到浅处理）           │   │
│   │                                                                │   │
│   │  Step 1: 叶子节点处理（LvMax → Lv0）                            │   │
│   │  ┌─────────────────────────────────────────────────────────┐   │   │
│   │  │ • 收集落入叶子空间范围的原始GLB文件                      │   │   │
│   │  │ • 直接合并GLB内容（保留原始几何，不做LOD简化）            │   │   │                
│   │  │ • 输出叶子节点GLB文件                                    │   │   │
│   │  └─────────────────────────────────────────────────────────┘   │   │
│   │                              ↓                                  │   │
│   │  Step 2: 父节点构建（分层独立优化）                             │   │
│   │  ┌─────────────────────────────────────────────────────────┐   │   │
│   │  │ ① 加载子节点GLB                                        │   │   │
│   │  │ ② 解包实例化扩展（unpackInstancing）                   │   │   │
│   │  │    将EXT_mesh_gpu_instancing展开为显式节点              │   │   │
│   │  │ ③ 合并所有子节点几何（mergeModel）                      │   │   │
│   │  │ ④ 网格简化（simplifyRatio=0.5）                        │   │   │
│   │  │ ⑤ 重新实例化判定（SemanticMaterialGeometricDetector）   │   │   │
│   │  │    - 对简化后的几何独立进行实例化检测                   │   │   │
│   │  │    - 不继承子层级实例组标识                              │   │   │
│   │  │ ⑥ 输出父节点GLB（含新识别的实例化）                     │   │   │
│   │  └─────────────────────────────────────────────────────────┘   │   │
│   │                              ↓                                  │   │
│   │  Step 3: 迭代直至根节点                                         │   │
│   │  （每层级独立进行简化-重建，形成分层LOD）                        │   │
│   │                                                                │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                              ↓                                           │
│   输出：自底向上构建的HLOD树 → 3D Tiles格式（tileset.json）             │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**分层独立优化策略核心思想**：

1. **空间分割与内容生成解耦**：先构建四叉树空间结构，再自底向上逐层生成内容
2. **层级独立实例化判定**：每层级独立调用`SemanticMaterialGeometricDetector`，不继承子层级的实例组标识
3. **简化-重建流程**：通过"解包→合并→简化→重新检测"四步，在父层级重新识别实例化机会
4. **几何相似性驱动**：实例化信息的纵向传递依赖简化后几何的一致性，而非标识映射

---

### 4.2 基于四叉树的空间层级划分

#### 4.2.1 空间分割策略与终止条件

**空间范围定义**：设BIM模型整体包围盒为 $\mathcal{B}_{world} = [x_{min}, x_{max}] \times [y_{min}, y_{max}] \times [z_{min}, z_{max}]$。

**四叉树分割规则**：在X-Z平面进行四叉树分割，Y方向保持完整（Y为高度轴）：

$$\text{Subdivide}(n) \rightarrow \{n_{bl}, n_{br}, n_{tl}, n_{tr}\}$$

子节点空间范围（Bottom-Left, Bottom-Right, Top-Left, Top-Right）：

$$\begin{aligned}
\mathcal{B}_{bl} &= [x_{min}, x_{mid}] \times [y_{min}, y_{max}] \times [z_{min}, z_{mid}] \\
\mathcal{B}_{br} &= [x_{mid}, x_{max}] \times [y_{min}, y_{max}] \times [z_{min}, z_{mid}] \\
\mathcal{B}_{tl} &= [x_{min}, x_{mid}] \times [y_{min}, y_{max}] \times [z_{mid}, z_{max}] \\
\mathcal{B}_{tr} &= [x_{mid}, x_{max}] \times [y_{min}, y_{max}] \times [z_{mid}, z_{max}]
\end{aligned}$$

**终止条件**（满足任一即停止分割）：

$$\text{Stop}(n) = \text{Depth}(n) \geq D_{max} \vee |C(n)| \leq N_{min} \vee \text{Area}(n) \leq A_{min}$$

其中：
- $D_{max}$：最大深度（如8层）
- $N_{min}$：最小构件数（如10个）
- $A_{min}$：最小面积（如25$m^2$）

**几何误差阈值分配**：设根节点几何误差为 $\epsilon_{root}$，子节点误差递减：

$$\epsilon(n_{child}) = \frac{\epsilon(n_{parent})}{2}$$

形成LOD层级：

| 层级 | 几何误差 | 内容精度 |
|------|----------|----------|
| LOD0 (Leaf) | $\epsilon_0$ | 完整细节 |
| LOD1 | $2\epsilon_0$ | 中等简化 |
| LOD2 | $4\epsilon_0$ | 高度简化 |
| LODn (Root) | $2^n\epsilon_0$ | 概览 |

#### 4.2.2 叶节点Tile的直接合并策略

叶节点是HLOD树的最深层节点，对应空间上最细粒度的分区。在自底向上构建策略中，叶节点采用**直接合并原始GLB**的方式生成内容：

**叶节点内容生成流程**：

1. **收集原始GLB文件**：遍历所有输入的原始GLB文件，根据构件中心点坐标判断其是否落入叶节点空间范围

$$\text{Objects}(n_{leaf}) = \{obj \in \text{InputGLBs} \ | \ obj.center \in \mathcal{B}(n_{leaf})\}$$

2. **直接合并GLB内容**：使用`mergeModel`函数将落入该叶节点的所有原始GLB文件合并为单一模型
   - 合并时保持原始几何数据不变
   - 不调用网格简化算法
   - 不额外进行实例化判定

3. **输出叶节点GLB**：将合并后的模型写入文件，命名为 `T{level}_{x}_{y}.glb`

**叶节点内容特点**：

```
LeafTile Content (T{L}_{X}_{Y}.glb):
├── 原始GLB几何数据（完整保留）
│   ├── Mesh A (来自原始文件1)
│   ├── Mesh B (来自原始文件2)
│   └── ...
└── 无LOD变体（仅单一精度）
    └── 注：LOD将在父层级通过简化-重建生成
```

**设计 rationale**：

- **简化延迟**：叶节点作为最精细层级，直接保留原始数据，避免在大量叶节点上进行重复简化计算
- **实例化延迟**：跨原始GLB文件的重复构件，在叶节点层级保持独立，在父层级合并简化后通过重新检测识别为实例
- **计算效率**：叶节点数量众多（四叉树叶子节点数为 $O(4^h)$），直接合并比逐层简化更高效

---

### 4.3 层级间实例重建与再识别机制

#### 4.3.1 子节点解包与几何聚合

在父节点构建阶段，首先加载所有子节点的GLB文件，将其几何内容合并为单一模型。

**步骤1：加载子节点GLB**

对父节点的每个子节点 $n_{child}$，加载其GLB文件：

$$\text{Models}_{children} = \{\text{LoadGLB}(n_{child}.tileFilename) \ | \ n_{child} \in \text{Children}(n_{parent})\}$$

**步骤2：解包实例化扩展（unpackInstancing）**

若子节点GLB中包含 `EXT_mesh_gpu_instancing` 扩展，需将其展开为显式节点，以便后续简化和重新检测：

$$\text{Unpacked}(M) = \text{ConvertInstancesToNodes}(M)$$

展开操作将每个实例从GPU实例化表示转换为独立的几何节点，保留变换信息：

- 从扩展中读取 `TRANSLATION`、`ROTATION`、`SCALE` 属性
- 为每个实例创建一个独立节点，应用相应变换
- 移除 `EXT_mesh_gpu_instancing` 扩展

**步骤3：合并几何**

使用 `mergeModel` 将所有解包后的模型合并：

$$G_{parent}^{raw} = \text{Merge}(\{\text{Unpacked}(M) \ | \ M \in \text{Models}_{children}\})$$

合并过程处理缓冲区、访问器、网格、材质等元素的索引偏移，确保所有几何数据正确聚合到单一模型中。

#### 4.3.2 网格简化与再实例化判定

**步骤4：网格简化**

对聚合后的几何模型应用简化算法，降低面数以生成父层级的LOD表示：

$$G_{parent}^{simplified} = \text{Simplify}(G_{parent}^{raw}, \text{target_ratio}=0.5)$$

简化参数：
- `target_ratio`：目标面数比例，默认为50%
- `error_threshold`：简化误差阈值，控制简化质量

**步骤5：重新实例化判定**

对简化后的几何，**独立**执行实例化检测流程，不继承子层级的实例组标识：

$$\text{DetectionResult} = \text{SemanticMaterialGeometricDetector}(G_{parent}^{simplified})$$

判定流程与第三章相同：
1. 计算几何签名与哈希
2. 语义预筛选
3. 容差精化判定
4. 识别新的实例组与非实例构件

**分层独立优化特性**：

不同于"实例保持"策略（维护标识映射），本方法采用"**实例重建**"策略：
- 每层级独立进行实例化判定
- 子层级的实例组标识不在父层级复用
- 父层级基于简化后的几何重新识别实例化机会

这种策略的优势：
- **自适应LOD**：不同层级可根据简化后的几何特征识别新的实例化模式
- **误差容限**：简化导致的微小几何变化不会破坏实例化关系（通过容差判定适应）
- **计算独立**：各层级实例化判定解耦，便于并行化

#### 4.3.3 分层独立优化的实例化效果

由于采用**分层独立优化**策略，父子层级间不存在显式的实例标识映射。实例化效果的传递通过几何相似性隐式实现：

**实例化传递的三种情形**：

| 子层级状态 | 简化后几何 | 父层级检测结果 | 效果描述 |
|------------|------------|----------------|----------|
| 多个相同实例 | 简化后仍相似 | 识别为新实例组 | 实例化关系延续 |
| 多个相似构件 | 简化后趋同 | 识别为新实例组 | 获得新的实例化机会 |
| 不同构件 | 简化后仍不同 | 保持非实例化 | 保持独立网格 |

**关键特性**：

1. **无标识继承**：父层级完全基于简化后的几何重新判定，不查询子层级的实例组信息
2. **容差自适应**：简化导致的轻微几何变形在容差范围内，大概率仍被识别为同一实例组
3. **层级特异性**：不同层级可能识别出不同的实例化模式（如高层级识别更大的聚合组）

**代码实现对应**：

```cpp
// 父节点构建流程（对应代码实现）
CesiumGltf::Model mergedModel;
for (auto& child : node->children) {
    auto model = LoadGLB(child->tileFilename);
    unpackInstancing(model);           // 解包实例化
    mergeModel(mergedModel, model);    // 合并几何
}

auto simplified = simplifyModel(mergedModel, 0.5);  // 网格简化

// 独立进行实例化判定（不传入子层级实例信息）
SemanticMaterialGeometricDetector detector(semanticParser, semanticHashFields, similarityThreshold, limit, hausdorffMaxSamplePoints, ...);
auto result = detector.detect({simplified});  // 重新检测

writeInstancedGlb(simplified, result, outputPath);  // 输出结果
```

---

### 4.4 跨瓦片实例聚合与数据组织

#### 4.4.1 瓦片边界处的实例分割问题

**问题描述**：几何相同的构件因跨越瓦片边界被分割到不同子节点，导致：

1. 各子节点需分别存储该实例组的部分实例
2. 父节点若不处理，将丢失实例化优势
3. 渲染时跨瓦片边界可能出现视觉不一致

**形式化描述**：设全局实例组 $I$ 的实例集合为 $\{inst_1, inst_2, ..., inst_n\}$，空间分布跨越多个叶节点：

$$\text{Distrib}(I) = \{n \in \text{Leaves} \ | \ \exists inst \in I: \text{Pos}(inst) \in \mathcal{B}(n)\}$$

若 $|\text{Distrib}(I)| > 1$，则该实例组在叶节点层级被分割。

#### 4.4.2 分层独立优化中的跨瓦片实例识别

在分层独立优化策略下，跨瓦片实例的识别不依赖全局注册表，而是通过父层级的**简化-重建**流程自然实现：

**跨瓦片实例识别机制**：

1. **子节点展开**：各子节点的实例化扩展被解包为独立几何节点
2. **跨子节点合并**：所有子节点的几何（包括来自不同子瓦片的相同构件）被合并到单一模型
3. **统一简化**：合并后的几何作为一个整体进行网格简化
4. **全局检测**：对简化后的几何执行实例化检测，此时跨子瓦片的相同构件处于同一检测空间

**效果**：若子瓦片A和子瓦片B各包含同一实例组的部分实例，在父层级：
- 这些实例被展开为独立几何
- 经简化后几何特征趋于一致
- 在重新检测时因几何相似性被识别为同一实例组
- 从而实现跨瓦片实例的聚合

#### 4.4.3 父层级跨瓦片实例统一表示

**分层独立优化的聚合效果**：

父层级的实例化检测结果自然包含跨子瓦片的实例聚合：

$$I_{parent} = \text{InstancingDetect}(\text{Simplify}(\bigcup_{i} \text{Unpack}(n_i)))$$

其中跨瓦片实例的变换矩阵被统一收集：

$$\text{Instances}(I_{parent}) = \{T_j \ | \ T_j \in \text{TransformsOf}(I_{source}, \text{AllChildren})\}$$

这种聚合方式的优势：
- **无全局状态依赖**：不需要维护跨层级的实例标识映射
- **自适应精度**：简化-检测流程容忍瓦片边界的微小几何差异
- **自然扩展**：新增瓦片无需更新全局注册表

**数据组织优化**：

```
ParentTile Content:
├── Cross-Tile Aggregated Instances
│   ├── InstanceGroup A (merged from Leaf1, Leaf2, Leaf3):
│   │   ├── Instance from Leaf1: [transform1, transform2]
│   │   ├── Instance from Leaf2: [transform3]
│   │   └── Instance from Leaf3: [transform4, transform5]
│   └── InstanceGroup B (merged from Leaf2, Leaf4)
├── Local Instances (within single child)
│   └── ...
└── Non-Instanced Geometry
    └── ...
```

---

### 4.5 实例与非实例混合的数据组织

#### 4.5.1 混合HLOD节点数据结构

**统一节点结构**：

```cpp
struct HLODNode {
    // 空间信息
    BoundingVolume boundingVolume;
    double geometricError;

    // 内容（混合存储）
    struct Content {
        // 实例化内容（GPU Instancing）
        std::vector<InstancedMeshGroup> instancedMeshes;

        // 非实例化内容（传统网格）
        std::vector<Mesh> nonInstancedMeshes;

        // 内容统计
        size_t totalInstances;
        size_t totalDrawCalls;
    } content;

    // 层级关系
    std::vector<HLODNode*> children;
    HLODNode* parent;
};
```

**内存与渲染效率分析**：

设节点包含 $n$ 个实例组，第 $i$ 组有 $k_i$ 个实例，几何复杂度为 $t_i$ 三角形。

- **纯实例化渲染代价**：$O(n)$ 绘制调用
- **完全展开渲染代价**：$O(\sum k_i)$ 绘制调用
- **存储节省率**：

$$\eta = 1 - \frac{\text{Size}_{instanced}}{\text{Size}_{expanded}} = 1 - \frac{\sum t_i + c \cdot \sum k_i}{\sum (t_i \cdot k_i)}$$

其中 $c$ 为每个实例的变换数据大小（通常16-24字节）。

#### 4.5.2 面向3D Tiles的层级输出格式

**3D Tiles 1.1规范映射**：

```json
{
  "asset": {"version": "1.1"},
  "geometricError": 100.0,
  "root": {
    "boundingVolume": {"region": [...]},
    "geometricError": 50.0,
    "refine": "REPLACE",
    "content": {
      "uri": "root.glb",
      "extensions": {
        "3DTILES_multiple_contents": {
          "contents": [
            {"uri": "root_instanced.glb"},
            {"uri": "root_merged.glb"}
          ]
        }
      }
    },
    "children": [
      {
        "boundingVolume": {"region": [...]},
        "geometricError": 25.0,
        "content": {"uri": "child_0.glb"}
      }
    ]
  }
}
```

**GLB内容组织**：使用 `EXT_mesh_gpu_instancing` 扩展存储实例化数据：

```json
{
  "extensions": {
    "EXT_mesh_gpu_instancing": {
      "attributes": {
        "TRANSLATION": 4,
        "ROTATION": 5,
        "SCALE": 6
      }
    }
  }
}
```

---

### 4.6 本章小结

本章针对复杂BIM模型的层次化组织需求，提出了**顾及多级LOD分层实例特征的HLOD构建方法**。主要贡献包括：

1. **形式化定义**了"多级LOD分层实例特征"概念，明确纵向重建（通过几何相似性）与横向聚合（通过空间合并）两个核心维度
2. 设计了**自底向上的四叉树空间分割与内容生成策略**：先进行XZ平面四叉树空间划分，再从叶子节点开始逐层向上生成LOD内容
3. 提出了**分层独立优化机制**：叶子节点直接合并原始GLB数据，父节点通过"解包→合并→简化→重新检测"四步流程独立进行实例化判定
4. 实现了**层级间实例重建策略**：不同于标识继承方式，本方法通过简化-重建流程在每一层级独立识别实例化机会，依赖几何一致性而非标识映射实现实例化信息的纵向传递
5. 设计了**实例与非实例混合的数据组织格式**，无缝映射到3D Tiles标准，兼顾渲染效率与规范兼容性

该方法与第三章的实例化判定形成完整技术链条：第三章提供单一层级的实例化检测能力，本章通过分层独立优化策略将其实际应用于HLOD的多层级构建中，共同支撑复杂BIM模型的高效轻量化与可视化。

---

## 附：公式符号对照表

| 符号 | 含义 |
|------|------|
|$m, m_i$ | BIM构件 |
|$M$ | 构件集合 |
|$V$ | 顶点集合 |
|$S$ | 几何表面 |
|$d_H$ | Hausdorff距离 |
|$\epsilon_g, \epsilon_v, \epsilon_n$ | 几何/顶点/法向量容差 |
|$\mathbf{M}, \mathbf{T}, \mathbf{R}, \mathbf{S}$ | 变换/平移/旋转/缩放矩阵 |
|$\mathcal{B}$ | 包围盒 |
|$\mathcal{H}$ | 哈希表 |
|$\mathcal{I}$ | 实例集合/注册表 |
|$n, n_i$ | HLOD节点 |
|$\mathcal{T}$ | HLOD树结构 |
|$I, I_i$ | 实例组 |
|$\epsilon_{geom}$ | 几何误差阈值 |
